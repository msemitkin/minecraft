# Журнал змін

Усі помітні зміни в MineClone документуються в цьому файлі.

## 2026-08-27

### Додано
- **Важіль і сигнальна линва: сигнал нарешті долає відстань.** Розглядалися
  три ідеї (важіль із линвою-проводом — двічі відкладена система передавання
  сигналу, якій 08-25 і 08-26 «личило прийти, коли буде що вмикати»;
  роздавач-турель — ще один «вихід», що дублює наявну оборону опудала й
  снігового голема та сам потребував би проводу; люк — варіація дверей без
  нової петлі, той самий аргумент, що відхилив підзорну трубу) — обрано
  важіль із линвою: входи (натискна плита) і виходи (двері, хвіртки, нотні
  блоки, динаміт) уже є, бракувало лише способу з'єднати їх на відстані.
  Нове дієслово — **провести сигнал**. Нуль зовнішніх ассетів (моделі,
  іконки й клац — процедурні):
  - **Важіль** — новий предмет (меню `Tab`): кам'яна основа з дерев'яним
    держаком і червоною головкою, ставиться `ПКМ` на тверду опору (межа —
    32). `ПКМ` по важелю плавно перекидає держак і **фіксує стан**: на
    відміну від плити, сигнал тримається без ваги, доки рука не клацне
    знову. Увімкнений важіль тримає сусідні двері й хвіртки відчиненими
    (відчинене рукою — не чіпає, як і плита), б'є по нотних блоках упритул
    і підпалює динаміт поруч.
  - **Сигнальна линва** — новий предмет (меню `Tab`): конопляний шнур на
    землі (межа — 192), що сам з'єднується рукавами із сусідніми линвами,
    важелями та плитами — і через сходинку вгору чи вниз. Увімкнене джерело
    живить весь з'єднаний ланцюг: линва **золотиться й іскрить**, а кожна
    запалена клітинка поводиться як натиснута плита — тримає двері/хвіртки,
    дзвонить у нотні блоки й підпалює динаміт. Фронт сигналу біжить шнуром
    із затримкою за відстанню: ряд нотних блоків уздовж линви грає
    **арпеджіо навздогін сигналу**.
  - Плита живить линву нарівні з важелем: дзвінок тепер дзвонить **у
    будинку**, коли гість стає на плиту біля воріт, а пастку зводять на
    безпечній відстані від принади.
  - Двері «пам'ятають руку»: мережа зачиняє лише те, що сама відчинила, і
    передає борг плиті чи мережі, якщо ту саму стулку тримають обидві.
  - Два нові досягнення — «🎚 Рубильник» (клацнути важелем) і «🔌 Сигнал у
    линві» (сигнал пробіг линвою три клітинки й спрацював); тепер **106**
    здобутків, мета-«Майстер MineClone» враховує обидва.
  - **Сумісність** — важелі (зі станом) і линви зберігаються зі світом;
    старі сейви працюють як були. Блок, що зайняв клітинку, чи втрачена
    опора розбирають механізм, як і плиту.
  - Дебаг: `MCDebug.giveLever()`, `giveWire()`, `placeLeverAt(x,y,z)`,
    `placeWireAt(x,y,z)`, `toggleLeverAt(x,y,z)`, `powerInfo`.

## 2026-08-26

### Додано
- **Скриня: перше сховище — запаси нарешті живуть у базі, а не губляться
  на межі капу торби.** Розглядалися три ідеї (скриня-сховище; важіль —
  другий «вхід» механізмів із фіксованим станом, але змінному сигналу
  личить прийти разом із линвою-проводом як окремому виміру, як уже
  вирішено 08-25; пташиний компаньйон-розвідник — ще один приручений
  супутник при вовках, конях і големах, найризикованіший з ідей по ШІ й
  найменш новий по суті) — обрано скриню: капи торби суворі (8 алмазів,
  3 корони, 16 золота, 16 борошна), і досі будь-який надлишок просто
  впирався в стелю — «Торба повна». Нове дієслово — **запасати**. Нуль
  зовнішніх ассетів (модель, іконка й рипіння віка — процедурні):
  - **Скриня** — новий предмет (меню `Tab`): дощаний короб із віком на
    завісах і залізною защіпкою, ставиться `ПКМ` на тверду опору (межа —
    16 скринь). `ПКМ` по скрині плавно відчиняє віко (рипіння завіс) і
    панель перекладання: **кожен вид краму лягає на власну полицю стосом
    до 64 штук** — 26 видів, від м'яса, меду та грибів до руд, перлин і
    корон. Кнопки «▼ 1 / ▼ все / ▲ 1 / ▲ все» сунуть крам в обидва боки;
    повернення в торбу шанує її кап («Торба повна — нікуди класти»).
  - **Добро не зникає мовчки** — `ЛКМ` розбирає лише **порожню** скриню
    (повна відмовляє: «спершу забери добро»); скриня, що втратила опору
    чи чию клітинку зайняв блок, розлітається, повертаючи вміст у торбу,
    скільки влізе, і чесно звітує, скільки розсипалося.
  - Два нові досягнення — «📦 Комора» (покласти крам на полицю) і
    «🧺 Повний стос» (зібрати стос із 64 одного краму); тепер **104**
    здобутки, мета-«Майстер MineClone» враховує обидва.
  - **Сумісність** — вміст кожної скрині зберігається зі світом; старі
    сейви працюють як були.
  - Дебаг: `MCDebug.giveChest()`, `placeChestAt(x,y,z)`, `openChestAt(x,y,z)`,
    `closeChest()`, `chestPutAt(x,y,z,kind,n)`, `chestInfo`.

### Виправлено
- **Плиту й нотний блок не можна було розібрати в світі без інших
  сутностей.** Зовнішня умова обробника `ЛКМ` по сутностях перелічувала
  смолоскипи, ковадла, вітряки тощо, але не знала про плити й нотні блоки:
  якщо в світі не було жодної іншої сутності, удар по плиті чи нотному
  блоку просто не спрацьовував. Умова тепер враховує плити, нотні блоки
  й нові скрині.

## 2026-08-25

### Додано
- **Нотний блок: музика нарешті стає дією, а механізми дістають голос.**
  Розглядалися три ідеї (нотний блок із дзвінком-сигналізацією; важіль із
  линвою-проводом — повноцінна система передавання сигналу, але це окремий
  великий вимір, і йому личить прийти, коли буде що вмикати; підзорна
  труба — чиста зручність огляду без нової петлі, той самий аргумент, що
  відхилив компас і ріг скликання) — обрано нотний блок: музику вже двічі
  відхиляли (джукбокс 08-17, музична скринька 08-23) як «декор без ігрової
  глибини», але нотний блок — не програвач, а інструмент: гравець сам
  настроює тон і складає звукоряд, а натиснута плита дістає перший «вихід»
  сигналу, окрім дверей і динаміту. Нове дієслово — **зіграти мелодію**.
  Нуль зовнішніх ассетів (модель, іконка й щипок струни — процедурні):
  - **Нотний блок** — новий предмет (меню `Tab`): дощана скринька з
    «мембраною» зверху й нотою на боці, ставиться `ПКМ` на тверду опору.
    `ПКМ` по блоку крутить тон на пів тону вгору по колу — **25 нот, дві
    октави від фа-дієза** (стрій нотного блока Minecraft) — і одразу грає
    його: дзвінкий щипок «арфи», блок пружно підстрибує, а над ним злітає
    нота в колір висоти звуку. Підказка називає ноту по-українськи
    («Соль♯ · тон 4/25»). `ЛКМ` — розібрати.
  - **Плита б'є по блоку** — натиснута плита в радіусі 2 клітинок ударяє
    по всіх нотних блоках довкола: сусідні грають одразу, дальші — з
    крихітною затримкою за відстанню, тож ряд блоків уздовж стежки грає
    **арпеджіо**. Плита при порозі — **дверний дзвінок**, плита при мурі —
    **сигналізація**: серед кривавої ночі нечисть, що суне до бази, сама
    сповістить про себе нотою.
  - Два нові досягнення — «🎵 Настроювач» (настроїти тон) і
    «🔔 Сигналізація» (плита без гравця сама вдарила по блоку); тепер
    **102** здобутки, мета-«Майстер MineClone» враховує обидва.
  - **Сумісність** — нотні блоки з тонами зберігаються зі світом; старі
    сейви працюють як були.
  - Дебаг: `MCDebug.giveNote()`, `placeNoteAt(x,y,z,pitch)`,
    `tuneNoteAt(x,y,z)`, `ringNoteAt(x,y,z)`, `noteInfo`.

### Виправлено
- **Табличку й дикий гриб більше не можна поєднати з плитою в одній
  клітинці.** Перевірки вільної клітинки таблички (`signCellFree`) та
  гриба (`mushCellFree`) не знали про натискні плити — табличка ставилася
  просто в плиту (і навпаки гриб міг прорости в ній). Обидві перевірки
  тепер враховують і плити, і нові нотні блоки.

## 2026-08-24

### Додано
- **Гарбузи, ліхтар Джека та гарбузова маска: у грі з'явився стелс — повз
  нечисть уперше можна прокрастися.** Розглядалися три ідеї (гарбузи з
  вирізанням ліхтаря та маскою-перевдяганням; небесні острови з лутом —
  дублюють дієслово «натрапити», щойно введене штольнями 08-22, тим самим
  аргументом, що відхилив затонулий корабель; важіль із линвою-проводом —
  розвиток механізмів, але дієслово «автоматизувати» ввели плити лише вчора,
  і вимір заслуговує визріти) — обрано гарбузи: досі кожна зустріч із
  нечистю мала рівно дві відповіді — бій (меч, щит, обладунок) або втеча,
  а обману не існувало зовсім. Нове дієслово — **маскуватися**. Нуль
  зовнішніх ассетів (текстури атласу, іконка, оверлей і звуки — процедурні):
  - **Гарбузи** — новий воксельний блок: дикі гарбузи детерміновано ростуть
    на трав'янистих луках рівнин (як кактуси в пустелі — і не під деревом,
    і не над входом у печеру); блок також доступний у меню Tab.
  - **Ліхтар Джека** — `ПКМ` по гарбузу вирізає обличчя: трикутні очі та
    зубата усмішка сяють зсередини власним вогнем навіть глупої ночі (грані
    рендеряться повнояскравим матеріалом лави) і **відлякують нечисть**, як
    смолоскип (радіус 8 проти 7; кривавої ночі — 3.5). Ліхтар не купиш у
    меню — лише вирізати; реєстр клітинок зберігається зі світом через
    правки (той самий прийом, що в зоряного каменю).
  - **Гарбузова маска** — новий предмет (меню Tab): `ПКМ` вдягає чи знімає
    (приціл не потрібен). У масці нечисть **приймає гравця за свого** й
    помічає лише з 6 блоків замість 26 — зомбі не женеться, кріпер не
    запалює ґніт, скелет не натягує лук, павук не стрибає. Плата — огляд:
    темні краї прорізів звужують поле зору (оверлей-віньєтка). **Ватажка
    облоги не обдуриш** (його 48 блоків чуття лишаються), а **зачеплена
    ударом нечисть пам'ятає кривдника** й більше не ведеться на маску —
    вдарив зомбі, тікай чесно.
  - Три нові досягнення — «🎃 Різьбяр» (вирізати ліхтар), «🎭 Маскарад»
    (вдягнути маску) і «🥷 Свій серед нечисті» (у масці прокрастися
    впритул до нечисті непоміченим); тепер рівно **100** здобутків,
    мета-«Майстер MineClone» враховує всі три.
  - **Сумісність** — старі сейви працюють як були: дикі гарбузи виростуть
    лише в незайманих чанках, стан маски зберігається з гравцем, вирізані
    ліхтарі — у правках світу.
  - Дебаг: `MCDebug.givePumpkin()`, `giveMask()`, `wearMask()`,
    `carveAt(x,y,z)`, `nearestPumpkin(r)`, `maskInfo`, `groundY(x,z)`,
    `clearMobs()`, `playerPos`.

## 2026-08-23

### Додано
- **Натискні плити: світ уперше відповідає на дію без кліку — з'явився
  перший механізм.** Розглядалися три ідеї (натискні плити — механізми,
  що реагують на вагу; джукбокс із процедурною музикою — нове дієслово
  «слухати», але самий лише амбіанс без ігрової глибини; затонулий корабель
  із лутом — дублює дієслово «натрапити», щойно введене штольнями 08-22) —
  обрано плити: досі геть усе в грі відбувалося лише від прямого кліку
  гравця, а механізмів — виміру, де світ сам реагує на події, — не існувало
  зовсім. Нове дієслово — **автоматизувати**. Нуль зовнішніх ассетів
  (модель, іконка та звуки — процедурні):
  - **Натискна плита** — новий предмет (меню Tab): дощаний обідок із
    кришкою, ставиться `ПКМ` на тверду опору. Вага зверху — гравець,
    нечисть, тварина чи вагонетка — просаджує кришку з клацом і вмикає
    плиту; коли вага сходить (із невеликою затримкою), плита клацає вдруге
    й вимикається. `ЛКМ` — розібрати; без опори чи під поставленим блоком
    плита спадає сама.
  - **Автоматичні двері** — натиснута плита відчиняє сусідні зачинені
    двері та хвіртки (радіус 1 клітинка по горизонталі, ±1 по вертикалі)
    і тримає їх, поки не зійде вага: двір, що сам впускає господаря, чи
    загін, що сам зачиняється за худобою. Відпущена плита дочиняє лише те,
    що відчинила сама (відчинене рукою не чіпає), шанобливо: крізь істоту
    в проймі двері не зачиняються — плита дочекається вільного проходу;
    двері, які тримає інша натиснута плита, теж лишаються відчиненими.
  - **Пастка з динамітом** — натискання підпалює динаміт упритул до плити
    (куб 3×3×3 довкола, зокрема й під нею) з коротким ґнотом: плита при
    мурі плюс схований заряд — і нечисть, що суне до бази, сама здіймає
    себе в повітря.
  - Два нові досягнення — «⚙ Механік» (поставити плиту) і «🪤 Пастка!»
    (нечисть сама наступила на плиту); тепер **97** здобутків,
    мета-«Майстер MineClone» враховує обидва.
  - **Сумісність** — плити зберігаються зі світом; старі сейви працюють
    як були.
  - Дебаг: `MCDebug.givePlate()`, `placePlateAt()`, `placeDoorAt()`,
    `placeGateAt()`, `plateInfo`.

## 2026-08-22

### Додано
- **Покинуті штольні: надра нарешті розповідають історію, а світ дістає
  першу згенеровану споруду.** Розглядалися три ідеї (ягідні кущі — ще одне
  відновлюване джерело їжі в лісі; залізний голем — міцніший вартовий двору;
  покинуті штольні в надрах) — обрано штольні: ягоди стали б черговим
  лічильником їжі одразу після того, як хліб увінчав харчову вершину
  (08-18), — той самий аргумент, що відхилив сироварню й компостер; залізний
  голем дублює дієслово сніговика («зліпи собі варту») дорожчим коштом, а
  оборона — найзабезпеченіша система гри (вовки, сніговики, щит, обладунок,
  громовідвід) — аргумент, що вже відхиляв залізні шипи. Штольні ж
  відкривають новий вимір: досі світ генерував лише рельєф, дерева й
  кактуси — жодної споруди, жодного сліду тих, хто копав тут до гравця, — а
  глибина платила самою сировиною (руди, гриби) і карала лавою. Нове
  дієслово — **натрапити**: стіна печери чи власний прохід раптом
  відкривається в підперту колодами галерею. Нуль зовнішніх ассетів
  (споруда складена з наявних процедурних блоків):
  - **Генерація** — детермінована ґратка 112×112 (шанс 55% на клітинку)
    розкладає штольні глибоко в камені (підлога y=11–20 — над лавовим морем
    печер): центральна зала 7×7×3 із дощаною підлогою та до чотирьох
    галерей 3×3 завдовжки 10–24 блоки з рамами з колод і дощаних балок
    кожні п'ять кроків. Штольня однаково виростає в усіх чанках, які
    перетинає (той самий прийом, що зшиває крони дерев), тож межі чанків
    непомітні.
  - **Обережна з довкіллям** — не підкопується ближче 4 блоків до поверхні
    (щоб хід не відкривався в озеро чи під ноги) і не чіпає воду та лаву;
    печери чесно перетинають галереї — так штольню й знаходять.
  - **Скриня припасів** — у центрі зали стоїть скриня: `ЛКМ` розбий — і
    припаси копачів минулого підуть у торбу: 3–6 ⚫ вугілля, 2–4 ⛓ заліза,
    1–2 🟡 золота, 2–4 🦴 кістки, 2–3 🕸 павутини, з шансом 35% — 💎 алмаз
    і 25% — 🍏 золоте яблуко (усе в межах лічильників торби). Луут живить
    одразу кузню, ферму й повідець. Згенеровану скриню не «надрукуєш» через
    постав-вибий: вона відрізняється від поставленої гравцем тим, що її
    нема в edits — та сама перевірка, що боронить руди; скриня мапи скарбів
    так само лишається окремою нагородою.
  - **Сумісність** — старі сейви працюють як були: правки гравця
    накладаються поверх генерації, тож зміняться лише незаймані надра.
  - Два нові досягнення — «🏚 Покинута штольня» (натрапити на штольню) і
    «📦 Припаси копачів» (забрати скриню); тепер **95** здобутків,
    мета-«Майстер MineClone» враховує обидва.
  - Дебаг: `MCDebug.shaftInfo`, `nearestShaft()`, `tpToShaft()`,
    `digShaftChest()`.

## 2026-08-21

### Додано
- **Гартування клинка в лаві: стихія-кара нарешті платить, а кузня дістає
  вершину понад алмазною стелею.** Розглядалися три ідеї (компостер —
  надлишки рослин перегнивають на добриво; крижані озера тундри з ковзанням;
  гартування алмазного клинка в лаві до полум'яного) — обрано гартування:
  компостер став би третьою поспіль спорудою-конвертером (вітряк 08-18,
  казан 08-19) і дублює наявний шлях кістяного борошна новим лічильником без
  нового дієслова у світі; крига — пасивна фізика без дії гравця, та й
  пересування — найзабезпеченіша система гри (аргумент, що двічі відхиляв
  санчата). Гартування ж дає нове дієслово — **загартувати клинок** — і
  закриває одразу дві діри: лава досі була єдиною стихією, що лише карає
  (вода давно платить устрицями й перлами, печери — грибами, гроза має
  громовідвід), а бойова кузня впиралася в алмазну стелю без продовження.
  Ризик у стилі зцілення зомбі — підійти до пекла впритул, та ще й із
  таймінгом на два кроки. Нуль зовнішніх ассетів (жар, пара, сичання й
  полум'я — процедурні):
  - **Розжарення** — з алмазним мечем при поясі `ПКМ` по **джерелу лави**
    (за 3 ⚫ вугілля роздмухати жар): клинок спалахує білим жаром у руці,
    куриться димком та іскрами, а жар тане — **25 секунд** до охолоння.
    Відро в руці головніше: з ним флюїди лишаються за відром, як і були.
  - **Гарт** — `ПКМ` по **джерелу води** (чи просто пірнути з розжареним
    клинком), поки жар не згас: пара, сичання — і **полум'яний клинок**
    назавжди жевріє помаранчем. Прогаяний жар холоне надарма — вугілля
    згоріло, спробуй ще; смерть теж гасить розжарений клинок (полум'яний
    гарт — назавжди, він лишається).
  - **Полум'я в бою** — удари полум'яного клинка **підпалюють нечисть**:
    вогонь догорає кілька секунд періодичною шкодою з язиками полум'я — і
    на зомбі зі скелетом, і на павуку з кріпером, яких не бере світанкове
    сонце, і на самому ватажку облоги. Вода під ногами чесно гасить,
    зомбі посеред зцілення золотим яблуком вогонь береже (за нього вже
    сплачено), а тварин полум'я милує — полювання лишається полюванням.
  - **Кузня знає межу** — новий рядок «🔥 Гартування» в розділі меча
    підказує, що цей ступінь кується не на ковадлі, а в полі (лава →
    вода), і показує стан: скільки жару лишилось чи що клинок уже
    загартований. Довідка керування (десктоп і сенсор) розповідає про
    новий ланцюжок.
  - Полум'яний гарт **зберігається зі світом** (старі сейви сумісні:
    звичайний меч, як і був).
  - Два нові досягнення — «🌡 Білий жар» (розжарити клинок у лаві) і
    «🔥 Полум'яний клинок» (загартувати у воді); тепер **93** здобутки,
    мета-«Майстер MineClone» враховує обидва.
  - Дебаг: `MCDebug.temperInfo`, `tryTemper()`, `heatNow()`, `quenchNow()`,
    `giveFlameSword()`.

## 2026-08-19

### Додано
- **Казан і зілля: далека здобич іде в алхімію, а гра дістає перші часові
  ефекти.** Розглядалися три ідеї (ворожий «утоплений» на дні водойм;
  сироварня — сир із коров'ячого молока; казан із варінням зіль) — обрано
  казан: водяний хижак знову карав би подароване пірнання за перлами (той
  самий аргумент, що вже тричі відхиляв «стихію-кару без винагороди»),
  сир — ще одна страва одразу після того, як хліб щойно увінчав харчову
  вершину (08-18), лічильник без нового дієслова. Казан же дає нове
  дієслово — **зварити зілля** — і закриває одразу дві діри: екзотична
  здобич далеких кутків (печерні гриби, пустельні опунції, донні устриці)
  вміла бути лише їжею чи крамом, а в грі не було жодного часового ефекту —
  тепер є буфи з таймером. Нуль зовнішніх ассетів (модель казана, іконки
  колб, бульбашки й сичання — процедурні):
  - **Казан 🍲** — нова споруда з меню (`Tab`), ставиться `ПКМ` на тверду
    землю (як ковадло чи вітряк): кам'яна основа, чавунний котел на ніжках
    і настоянка, що міняє колір за станом. `ЛКМ` — розібрати (незварена
    засипка чесно вертається в торбу, вода виливається); казан спадає,
    якщо зруйнувати опору чи зайняти клітинку блоком.
  - **Вода** — `ПКМ` відром із водою наповнює казан (відро вертається
    порожнім), а **дощ просто неба наповнює порожній казан сам** за
    25 секунд зливи — примирення з негодою.
  - **Варіння** — `ПКМ` по водяному казану кидає перший рецепт, на який
    стає запасу: **🍄 2 гриби → зілля нічного зору 🌙**, **🌵 2 опунції →
    зілля прудкості ⚡**, **🦪 2 устриці → зілля зябер 🫧**. Вариво булькає
    8 секунд кольоровими бульбашками, тоді настоянка **світиться готовим
    зіллям** і виблискує, чекаючи охочого.
  - **Дія** — `ПКМ` по готовому казану випиває зілля просто з черпака:
    **нічний зір** (90 с) тримає ніч на місячному порозі світла, **прудкість**
    (60 с) жене крок і біг у півтора раза швидше з іскрами під ногами,
    **зябра** (90 с) не дають повітрю танути під водою — пірнання за
    перлами без задухи. Таймери-чипи 🌙⚡🫧 із секундами тануть у HUD,
    збіглий ефект прощається підказкою; смерть змиває дію зіль.
  - Усе **зберігається зі світом**: казани з недовареним зіллям, дощова
    наповненість і недопиті секунди дії (старі сейви сумісні).
  - Два нові досягнення — «🧪 Зіллєвар» (зварити зілля) і «⚗ Дегустатор»
    (скуштувати всі три) — тепер **91** здобуток, мета-«Майстер MineClone»
    враховує обидва. Довідка керування (десктоп і сенсор) розповідає про
    новий ланцюжок.
  - Дебаг: `MCDebug.giveCauldron()`, `giveBrewStuff(n)`, `cauldronNear()`,
    `fillCauldron()`, `useCauldron()`, `brewFast()`, `potionInfo`.

## 2026-08-18

### Додано
- **Вітряк, борошно та хліб: жнива нарешті мають продовження, а ферма —
  власну вершину.** Розглядалися три ідеї (ворожий «утоплений» на дні
  водойм; санчата для швидкого спуску тундрою; вітряк із помелом борошна
  й випіканням хліба) — обрано вітряк: водяний хижак знову карав би щойно
  подароване пірнання за перлами (той самий аргумент, що вже двічі відхиляв
  «стихію-кару без винагороди»), санчата — третій транспорт поряд із човном
  і вагонеткою, розвага без нової економіки. Вітряк же дає нове дієслово —
  **змолоти** — і закриває давню діру: зернова ферма (посів, бджоли-запилювачі,
  кістяне борошно, опудало проти ворон) уся працювала на жменю зерна по
  2 «ніжки», не маючи власної кульмінації, як руда має кузню. Нуль зовнішніх
  ассетів (модель вітряка, іконки колоска/борошна/хліба, гуркіт жорен —
  процедурні):
  - **Колосок 🌾** — стиглий колос при зборі (`ЛКМ`) тепер, крім зерна,
    лишає колосок у торбу (бейдж-чип, до 32); перший колосок підказує
    поставити вітряк.
  - **Вітряк** — нова споруда з меню (`Tab`), ставиться `ПКМ` на тверду
    землю (як ковадло): кам'яний фундамент, дощана вежа, дашок і хрест
    полотняних крил, що **ліниво крутяться від вітру**. `ПКМ` по вітряку
    засипає **3 колоски** в жорна: крила розганяються, гуркочуть камені,
    сиплеться борошняний пилок — за 8 секунд у торбі **борошно 🥣** (до 16).
    `ЛКМ` — розібрати (недомелена засипка чесно вертається колосками);
    вітряк спадає, якщо зруйнувати опору чи зайняти клітинку блоком.
  - **Хліб 🍞** — борошно `ПКМ` кладеться на пласкі камені багаття (одна
    конфорка на всіх: устриця — перша, за нею опунція, тоді хлібина):
    тісто рум'яниться до скоринки й за 7 секунд жару спікається в хліб —
    **найситнішу страву гри (6 «ніжок»)**; `F` сам їсть його першим при
    великому голоді. Розібране багаття вертає недопечену хлібину борошном.
  - Усе **зберігається зі світом**: лічильники колосків/борошна/хліба,
    вітряки разом із незавершеним помелом і хлібина на каменях (старі сейви
    сумісні).
  - Два нові досягнення — «🌬 Мірошник» (змолоти борошно) і «🍞 Пекар»
    (спекти хліб); тепер **89** здобутків, мета-«Майстер MineClone» враховує
    обидва. Довідка керування (десктоп і сенсор) розповідає про новий
    ланцюжок.
  - Дебаг: `MCDebug.giveMill()`, `giveWheat(n)`, `giveFlour(n)`,
    `millNear()`, `grindWheat()`, `grindFast()`, `bakeBread()`,
    `breadFast()`, `grainInfo`.

## 2026-08-17

### Додано
- **Квіти та фарбування вовни: луки платять кольором, а будівничий дістає
  палітру.** Розглядалися три ідеї (човен — уже є в грі, тож насправді
  зважувалися: ворожа водяна нечисть — «утопленик», що стереже устричне дно;
  музична скринька з процедурними мелодіями; квіти з барвниками) — обрано
  квіти: водяний хижак карав би щойно подароване пірнання за перлами
  (устриці — свіжа петля 08-14, а «стихія-кара без винагороди» вже відхиляла
  пожежу), музична скринька — чистий декор без нової дії (аргумент, що
  відхилив ріг і компас). Квіти ж дають нове дієслово — **пофарбувати** — і
  закривають одразу дві діри: вовна, єдиний «м'який» блок гри, лишалася
  одноколірною, а луки рівнин і лісів — єдиним біомом без власної здобичі
  (печери платять грибами, дно — устрицями, пустеля — опунціями, ліс —
  трюфелями). Нуль зовнішніх ассетів (моделі квітів, іконки, текстури
  фарбованої вовни — процедурні):
  - **Луки цвітуть** — на траві рівнин і лісів просто неба проростають
    **мак** 🌹, **кульбаба** 🌼 і **волошка** 🌸 (світ засівається сам, як
    гриби в печерах: дикі квіти не туляться купою, не ростуть під дахом, а
    дальні тихо пересіваються ближче до гравця — луг не вичерпується).
    `ЛКМ` зриває квітку в торбу — свій лічильник на кожен вид (до 16),
    бейдж-чипи при торбі.
  - **Сад** — квітка з меню (`Tab`) садиться `ПКМ` на траву чи землю:
    посаджені квіти недоторканні для пересіву — довкола дому виростає
    власна клумба. Квітка спадає пелюстками, якщо зруйнувати ґрунт під нею
    чи зайняти клітинку блоком; усе **зберігається зі світом** (старі сейви
    сумісні — луки засіваються самі).
  - **Фарбування** — `ПКМ` квіткою по блоку вовни перефарбовує його в колір
    квітки (квітка витрачається): три нові воксельні блоки — **червона,
    жовта й синя вовна** — з тим самим кучерявим малюнком і приглушеним
    звуком кроків. У меню їх нема: єдиний шлях до кольору — луки чи сад,
    тож палітра заробляється прогулянкою.
  - **Бджоли люблять квіти** — вулик поряд із лугом чи клумбою наповнюється
    медом швидше (кожна квітка — як грядка: +12% темпу, разом до 8).
  - Два нові досягнення — «🌼 Квітникар» (зірвати квітку) і «🎨 Фарбар»
    (пофарбувати вовну); тепер **87** здобутків, мета-«Майстер MineClone»
    враховує обидва. Підказка при першій квітці; довідка керування (десктоп
    і сенсор) розповідає про нове.
  - Дебаг: `MCDebug.giveFlowers(n)`, `sproutFlowers()`, `flowerState()`,
    `pickNearestFlower()`, `paintWoolAt(x, y, z, kind)`, `flowerInfo`.

## 2026-08-16

### Додано
- **Зцілення зомбі: золоте яблуко повертає нечисті людську подобу, а
  торгівля дістає постійну адресу.** Розглядалися три ідеї (ріг скликання —
  сурма, що кличе приручених вовків і коней до гравця; кресало й жива
  пожежа — вогонь, що шириться деревом і травою та гаситься відром;
  зцілення зомбі золотим яблуком до осілого крамаря) — обрано зцілення:
  ріг — чиста зручність без нової петлі (аргумент, що вже відхилив компас
  і банери, — та й приручений вовк і так тримається поруч); пожежа —
  стихія-кара без винагороди, до того ж гроза щойно дістала зуби (08-15),
  а дощ, що супроводжує блискавку, гасив би власні підпали. Зцілення ж дає
  нове дієслово — **вилікувати ворога** — і закриває одразу дві діри:
  золоте яблуко, вершина всіх торгових ланцюжків гри, досі було чистим
  споживанням (з'їв — і по всьому), а сама торгівля лишалася заручницею
  випадку — гість приходить раз на день-два, коли сам схоче. Тепер
  найдорожчий харч стає інвестицією, а нічний бій — дорогою до постійної
  крамниці під боком. Нуль зовнішніх ассетів (модель селянина, дрож і
  висхідний передзвін — процедурні):
  - **Зцілення** — ПКМ по зомбі з золотим яблуком 🍏 у торбі (підходити
    треба впритул — ризик!): яблуко витрачається, зомбі здригається на
    місці в золотавому мерехтінні — не полює, не б'є і не горить удосвіта,
    а вовк і сніговик його не чіпають (за нього вже сплачено). За кілька
    секунд дрожі з передзвоном нечисть обертається на **зціленого
    селянина** в солом'яному брилі. Сейв посеред дрожі — втрачене яблуко
    (нечисть у сейв не потрапляє), але зцілення триває лічені секунди.
  - **Сусід** — селянин оселяється там, де завершилося зцілення: блукає
    садибою (радіус ~8 блоків), не деспавниться на відстані й
    **зберігається зі світом** (старі сейви сумісні). Сусід один: друге
    яблуко на зомбі не змарнувати, поки перший живий. Смертний, як усі, —
    від блискавки його береже хіба громовідвід, а вбитого не повернути:
    тільки зцілити іншого зомбі.
  - **Крамниця** — ПКМ по сусіду відкриває крамницю (та сама панель, що
    ятка гостя, зі своїм заголовком): три пропозиції з торгового пулу,
    щонайменше одна — із золотим яблуком, кожна діє двічі, а **щоранку
    крам свіжий**. Мандрівний торговець ходить, як і ходив: тепер він —
    шлях до першого яблука, а сусід — його окупність.
  - Підказка в ятці: назбиралося двоє яблук — гра шепне, що ними можна
    зцілювати; довідка керування (десктоп і сенсор) розповідає про нове.
  - Два нові досягнення — «💊 Зцілитель» (зцілити зомбі) і «🏘 Добрий
    сусід» (обмінятися в крамниці); тепер **85** здобутків, мета-«Майстер
    MineClone» враховує обидва.
  - Дебаг: `MCDebug.spawnZombieNear(dx, dz)`, `cureNearest()`, `cureNow()`,
    `openShop()`, `shopTrade(i)`, `refreshShop()`, `villagerInfo`.

## 2026-08-15

### Додано
- **Блискавки й громовідвід: гроза нарешті б'є по-справжньому, а двір
  вчиться від неї ховатися.** Розглядалися три ідеї (скриня-сховище —
  окремий склад для надлишків торби; вітряк і хліб — млин меле зерно на
  борошно, багаття пече буханці; блискавки з громовідводом — справжні
  розряди в грозу та щогла-захисник) — обрано блискавки: скриня не додає
  нової дії у світі (капи торби — свідомий баланс, а не тіснота), хліб —
  ще одна страва в і без того ситій кухні (м'ясо, зерно, гриби, опунції,
  трюфелі, мед, устриці). Гроза ж досі була декорацією: дощ шумів, спалахи
  миготіли, а грім не мав зубів — жодна система світу не била з неба.
  Тепер негода — небезпека, від якої будуються. Нова дія гравця —
  **поставити громовідвід і вкрити двір від грози**. Нуль зовнішніх
  ассетів (зиґзаґ розряду, тріск грому та іскри — процедурні):
  - **Справжні удари** — під час сильного дощу частина спалахів тепер
    розряди поблизу: блискавка цілить у найвищу з випадкових точок
    довкола гравця (вершини притягують!), б'є все живе в радіусі
    3 блоків на 6 шкоди й підкидає. Стихію панцир не спиняє — як вогонь,
    падіння і воду: обладунок від неба не рятує.
  - **Сліди на землі** — розряд у пісок обертає його на скло
    (фульгурит!), трава вигоряє до голої землі; на місці удару — сніп
    іскор і дим.
  - **Громовідвід** — нова щогла в меню (Tab): залізна жердина з золотим
    вістрям, ставиться на тверду землю за 2 ⛓ заліза + 1 🟡 золото з
    торби (ЛКМ розбирає — метал вертається). Перехоплює розряди в
    радіусі 12 блоків і безпечно заземлює: ані шкоди, ані слідів — лише
    сніп іскор, а вістря ще хвилину жевріє. Під дахом щогла не працює:
    небо над вістрям мусить бути відкритим.
  - Далекі зірниці лишилися — частина спалахів, як і раніше, тільки
    підсвічує небо (грім докочується із запізненням), тож гроза не
    перетворюється на суцільний артобстріл.
  - Громовідводи **зберігаються зі світом** (старі сейви сумісні).
  - Два нові досягнення — «🌩 Іскра в жилах» (пережити удар блискавки) і
    «⚡ Приборкувач грози» (громовідвід упіймав розряд); тепер **83**
    здобутки, мета-«Майстер MineClone» враховує обидва.
  - Довідка керування (десктоп і сенсор) розповідає про нову щоглу.
  - Дебаг: `MCDebug.strikeNow(x?, z?)`, `spawnRodNear(dx, dz)`,
    `giveRodMetal(n)`, `giveLightningRod()`, `rodInfo`.

## 2026-08-14

### Додано
- **Обладунок і лагодження: тіло нарешті під захистом, а шахта — при
  ділі й після алмазної кирки.** Розглядалися три ідеї (зілля на багатті —
  відвари з грибів, меду й трюфелів із часовими ефектами; кристальні
  печери — сяйні жеоди в надрах як нове джерело світла й крам; обладунок —
  пасивний захист тіла з міцністю та лагодженням) — обрано обладунок:
  зілля тягне за собою цілу систему часових бафів із власним HUD, а
  регенерацію вже чесно закривають їжа й золоті яблука; кристали —
  прикраса без нової дії у світі (надра й так платять рудами і скарбом,
  а світло дають смолоскипи). Обладунок же закриває останню діру бойового
  трикутника: меч (08-08) — напад, щит (08-09) — активний блок, а
  пасивного захисту тіла нема — ватажок (08-13) б'є на 6, його хвиля
  обходить щит, і жодна броня не пом'якшує удару. Заразом лагодження дає
  шахті сенс після алмазної кирки: досі руди були потрібні лише на
  одноразові кування, тепер панцир треба утримувати металом. Нова дія
  гравця — **носити і лагодити панцир**. Нуль зовнішніх ассетів (іконка,
  брязкіт і скрегіт — процедурні):
  - **Кузня** — четвертий розділ «🦺 Обладунок» на ковадлі: **залізний**
    (3 вугілля + 4 залізо, −2 шкоди з удару, міцність 20) → **золотий**
    (3 вугілля + 4 золота, −3, міцність 28) → **алмазний** (4 вугілля +
    3 алмази, −4, міцність 36). Кується послідовно, як кирка й меч.
  - **Захист** — кожен удар нечисті, стріла скелета, вибух динаміту чи
    кріпера, метеорит і рій бджіл б'ють слабше на absorb рівня, але
    завжди лишають бодай 1 шкоди; навіть хвиля ватажка, яку не спиняє
    щит, у панцирі м'якша. Стихії панцир оминають: падіння, вогонь,
    голод і вода б'ють як били — обладунок не скасовує обережності.
  - **Міцність** — прийнятий удар знімає 1 міцності (брязкіт пластин,
    іскри); на нулі панцир **розколюється** (скрегіт) і не захищає, доки
    не полагоджений. Бейдж 🦺 при торбі показує залишок міцності й
    тьмяніє в розбитого.
  - **Лагодження** — окремий рядок кузні «🧰 Лагодження»: за метал свого
    рівня (2 заліза / 2 золота / 1 алмаз) панцир відновлюється до повної
    міцності — шахта тепер потрібна постійно, а не до першого алмаза.
  - Рівень і міцність обладунку **зберігаються зі світом** (старі сейви
    сумісні: без панцира, як і були).
  - Два нові досягнення — «🦺 Броньований» (скувати обладунок) і
    «🧰 Лагодій» (полагодити пошарпаний панцир); тепер **81** здобуток,
    мета-«Майстер MineClone» враховує обидва.
  - Дебаг: `MCDebug.forgeArmorNext()`, `mendArmor()`, `setArmorHp(n)`,
    `hurtBy(n, cause)`, `armorInfo`.

### Виправлено
- **Підказки кузні наздогнали четвертий розділ.** Заголовок панелі кузні
  та рядки ковадла в довідці керування (десктоп і сенсор) досі обіцяли
  лише «кирку, меч і щит» — тепер згадують і обладунок із лагодженням.
  Заразом рядок «🧰 Лагодження» перемальовується одразу, коли панцир
  приймає удар при відкритій кузні (міцність у рядку більше не застаріває).

## 2026-08-13

### Додано
- **Ватажок кривавої ночі: облога нарешті має кульмінацію, а кузня —
  іспит.** Розглядалися три ідеї (сани для тундри — швидкий спуск
  засніженими схилами; банери-дороговкази, що позначають свої місця на
  мінімапі; бос-ватажок кривавої ночі) — обрано ватажка: сани — четвертий
  транспорт при живих коні, човні й вагонетці (пересування — і так
  найзабезпеченіша система гри: є ще гак і політ), та й розвага без петлі —
  з гори з'їхав, а назад пішки; банери — чиста зручність без нової дії у
  світі (тим аргументом уже відхиляли килими з фарбами), до того ж світ уже
  анотується табличками, а скарбна мапа й так малює маркер. Ватажок же
  закриває найпомітнішу діру гри: бойова гілка викута до стелі (меч 08-08,
  щит 08-09), а мірятися їй нема з ким — звичайний зомбі падає з одного
  алмазного удару; кривава ніч (07-27) — єдина подія-виклик — досі без
  кульмінації, її можна пересидіти пасивно за парканом. Тепер облога має
  господаря, бій із яким — активна дуель із ризиком, а перемога платить
  короною — новим верхом торгової драбини (як перлина — з дна, як трюфель —
  із лісу). Нова дія гравця — **дуель із босом**. Нуль зовнішніх ассетів
  (модель, рик, струс — процедурні):
  - **Прихід** — раз на криваву ніч, за пів хвилини після її початку, з
    пітьми виходить **ватажок** — здоровенний зомбі в золотій короні, з
    кістяними наплічниками й багряними очима (рев і тост сповіщають про
    прихід). Його **не спиняє світло** смолоскипів і багать, він **не
    деспавниться** на відстані й чує гравця вдвічі далі за звичайну
    нечисть; угорі екрана — **смуга здоров'я боса**, поки той поряд.
  - **Бій** — ватажок повільніший за зомбі, але б'є важко (6 шкоди, далекий
    відкид; піднятий щит фронтальний удар чесно відбиває), а зблизька
    **гатить по землі**: мить замаху (руки над головою — сигнал відбігти) —
    і ударна хвиля ранить і підкидає, **щит її не спиняє** — рятує лише
    дистанція. Вовки й сніговики чесно втручаються в дуель, а на світанку
    недобитий ватажок згоряє, як і решта нечисті (нова причина смерті —
    «Розчавлений ватажком облоги»).
  - **Корона 👑** — повалений ватажок лишає **корону** (і жменю кісток):
    золотий вінець виблискує іскрами на землі (лежить 4 хв — удвічі довше
    за кістки) і підбирається впритул у торбу (лічильник 👑, до 3,
    зберігається зі світом). У ятці торговця корона — **найдорожчий крам
    гри: 1 👑 → 2 золоті яблука 🍏** (перлина й трюфель дають по одному);
    рядок торби в ятці показує й корони (старі сейви сумісні).
  - Перерваний сейвом бій чесно продовжується: живий ватажок після
    завантаження приходить знову, а вже повалений — ні (прапорець у сейві).
  - Два нові досягнення — «🩸 Ватажка повалено» (здолати боса) і «👑 Корона
    облоги» (підібрати корону); тепер **79** здобутків, мета-«Майстер
    MineClone» враховує обидва.
  - Дебаг: `MCDebug.spawnWarlord()`, `killWarlord()`, `slamNow()`,
    `giveCrown(n)`, `warlordInfo`.

### Виправлено
- **Порожні бейджі 🌵 🍠 🌰 більше не висять нулями на екрані.** Бейджам
  плодів опунції, печеної опунції та трюфеля бракувало явного правила
  `[hidden] { display: none }` — авторський `display: flex` перекривав
  браузерне ховання, тож порожні лічильники лишалися видимими. Заразом ці
  бейджі (і нову корону) додано до списку зсуву лівіше на сенсорних
  пристроях, щоб не лягали на кнопки керування.

## 2026-08-12

### Додано
- **Трюфельна свиня: у свині нарешті є професія, а в повідця — другий
  сенс.** Розглядалися три ідеї (замовлення торговця — контракт «принеси
  до сутінок»; морозні ягоди в тундрі, що їх відігрівають вогнем; свиня,
  що винюхує трюфелі на повідці) — обрано свиню: замовлення — мета-шар
  поверх наявних петель збору без нової дії у світі (тим аргументом уже
  відхиляли досвід із рівнями), та й ятка реактивно росте з кожним релізом
  сама; морозні ягоди — третій поспіль реліз «біом платить їжею, що
  збирається» (устриці 08-10, опунція 08-11), а тундра й так платить
  снігом для сніговика-вартового. Свиня ж закриває останню діру
  тваринництва: вона — **єдина свійська тварина без живого продукту**
  (корова доїться, вівця стрижеться, курка несеться — свиня досі лише
  м'ясо). Повідець дістає другий сенс — не лише **привести** знайдену
  худобу додому, а й **вивести** свою на промисел; нова дія гравця —
  **вигул свині лісом**. Заразом ліс, найстаріший біом, дістає власну
  сховану здобич (як печери — гриби, дно — устриці, пустеля — опунцію),
  а нічне полювання на павуків (павутина повідця) живить тепер і лісовий
  промисел. Нуль зовнішніх ассетів (модель, іконка, рохкання —
  процедурні):
  - **Нюх** — свиня на повідці (доросла), виведена в **ліс**, дорогою
    нюшить землю: час від часу чує схований під деревами трюфель —
    звертає до місця **біля стовбура** (між коренів), риє його п'ятачком
    ~3 с (ніс у землі, летить земля, чутно рохкання) і **викопує
    трюфель**. Між знахідками свиня перепочиває 40–65 с; гравець відійшов
    убік — облишає риття і наздоганяє поводиря (повідець не рветься об
    нюх). Поза лісом нюх мовчить — разова підказка спрямує до хащ.
  - **Трюфель 🌰** — темна горбкувата грудка погойдується на землі,
    підбирається впритул у торбу (новий лічильник 🌰, до 8; невзятий
    зникає за 2 хв, як кістки). Делікатес: `F` з'їдає його **останнім**,
    коли решта харчу скінчилась (3 «ніжки»), бо головна його ціна — у
    ятці.
  - **Торговець знає смак лісу** — у пулі пропозицій тепер
    **2 🌰 → золоте яблуко 🍏** та **1 🌰 → 2 меди 🍯** (трюфель —
    перлинна ліга: найкоротший шлях до головного призу з суходолу).
    Рядок торби в ятці показує й трюфелі. Лічильник 🌰 зберігається зі
    світом (старі сейви сумісні).
  - Два нові досягнення — «🐽 Трюфельний нюх» (свиня винюхала трюфель) і
    «🌰 Лісовий делікатес» (з'їсти трюфель); тепер **77** здобутків,
    мета-«Майстер MineClone» враховує обидва.
  - Дебаг: `MCDebug.giveTruffles(n)`, `tpToForest()`, `spawnLeashedPig()`,
    `sniffNow()`, `digNow()`, `truffleInfo`.

### Виправлено
- **Бейдж плодів опунції оновлюється одразу після обміну в ятці.**
  `doTrade` не оновлював лічильник 🌵 на екрані після обміну плодів —
  бейдж показував стару кількість до наступної зміни торби. Знайдено при
  інтеграції трюфелів у ятку.

## 2026-08-11

### Додано
- **Плоди опунції: пустеля нарешті платить за похід, а далекобій отримує
  мирне діло.** Розглядалися три ідеї (кіт-приблуда, що відлякує кріперів;
  ткацтво — вовна зі стрижки на килими та прапори; плоди опунції на
  кактусах, що їх збивають пострілом) — обрано опунцію: кіт — пасивна
  захисна аура без нової дії гравця після приручення (тим аргументом уже
  відхиляли обладунки й шипи), та й оборона — найбагатша система гри;
  ткацтво впирається в те, що блок вовни вільно ставиться з меню — вовна зі
  стрижки «друкувала б» безкоштовну сировину (той самий запобіжник, що
  береже руду), а килими — декор без петлі (аргумент, що відхилив фарби);
  опунція ж закриває останній «неоплачений» біом: печери платять грибами
  (08-03), дно — устрицями (08-10), а пустеля досі лише карала кактусовими
  колючками й нічого не дарувала. Заразом лук і сніжка дістають **перший
  мирний ринок** (як руда — меч услід за киркою, як павутина — гак услід за
  повідцем): досі далекобій умів лише ранити. Нова дія гравця — **збити
  ціль пострілом**. Нуль зовнішніх ассетів (моделі, іконки, звук —
  процедурні):
  - **Зав'язь** — у пустелі на **верхівках кактусів** неподалік гравця час
    від часу зав'язується рожевий плід опунції на зеленій подушці (до 6
    диких поблизу; коли глобальний запобіжник у 64 заповнено, найдальший
    плід поза очима тихо пересівається ближче — знахідки не вичерпуються;
    спавн серед пустелі засіює кактуси одразу). Зрубаний під плодом кактус
    чи зайнята клітинка — плід спадає хмаркою бризок, як гриб без ґрунту.
  - **Збивання** — плід на кактусі **збивають стрілою чи сніжкою**: він
    падає під кактус у бік польоту снаряда, погойдується на землі та
    підбирається впритул у торбу (новий лічильник 🌵, до 16; в'яне за 2 хв,
    як кістки). `ЛКМ` голіруч теж зриває, але **колючки жалять** на 1 серце
    (нова причина смерті «Поколовся об кактус» — і підказка, що плід можна
    збити пострілом).
  - **Печена опунція** — сирий плід соковитий (`F`, 2 «ніжки» — між м'ясом
    і грибом), а `ПКМ` по багатті кладе його на **пласкі камені біля вогню**
    у спільну з устрицею чергу (устриця з дна — перша): за ~5 с жару з
    солодким димком плід карамелізується в **печену опунцію 🍠** (нова
    ситна страва на 4 «ніжки»; `F`, як завжди, сам обирає наїдок без
    марнування). Розібране посеред печення багаття вертає плід сирим.
  - **Торговець знає смак пустелі** — у пулі пропозицій тепер
    **5 🌵 → золоте яблуко 🍏** та **3 🌵 → 2 меди 🍯**; рядок торби в ятці
    показує й плоди. Лічильники 🌵 🍠 зберігаються зі світом, як і плоди на
    кактусах (старі сейви сумісні — їхня пустеля засіється сама).
  - Два нові досягнення — «🎯 Влучний збір» (збити плід пострілом) і
    «🍠 Печена опунція» (спекти плід на багатті); тепер **75** здобутків,
    мета-«Майстер MineClone» враховує обидва.
  - Дебаг: `MCDebug.giveFruit(n)`, `giveBaked(n)`, `sproutFruits(tries)`,
    `tpToDesert()`, `tpToFruit()`, `knockNearestFruit()`,
    `pickNearestFruit()`, `bakeFruit(fast)`, `fruitState()`, `fruitInfo`,
    `tp(x, z)`.

## 2026-08-10

### Додано
- **Устриці та перли: дно водойм нарешті платить за спуск.** Розглядалися
  три ідеї (дикі вовки — нічні нальотчики на загін худоби; планер із
  павучого шовку; устриці на дні, що їх пара розкриває на перли) — обрано
  устриці: вовчий наліт повторив би щойно освоєну воронами петлю нальоту
  без нової дії гравця (оборона і так готова — паркан, повідець, приручений
  вовк; тим самим аргументом свого часу відхиляли лиса), планер — третя
  поспіль увага дузі пересування (гак щойно в минулому релізі) і третій
  ринок шовку за два дні; а устриці дають воді те, що гриби дали печерам, —
  винагороду за спуск: запас повітря досі був чистим покаранням (пірнати
  було нізащо), тепер **пірнання** — це промисел. Дуга води найстаріша з
  недоплачених (риболовля та пляшка з мапою — останні її релізи), а мушля
  на багатті сплітає одразу три системи: воду, кухню й ятку торговця. Нуль
  зовнішніх ассетів (моделі, іконки, звуки — процедурні):
  - **Проростання** — на затопленому дні (над мушлею щонайменше два блоки
    води — по мілководдю не находишся) неподалік гравця час від часу
    **проростає устриця**: прочинені сірі стулки з проблиском м'якуша, що
    зрідка пускають зрадливу **бульбашку** (до 8 диких поблизу; коли
    глобальний запобіжник у 96 заповнено, найдальша устриця поза очима
    тихо пересівається ближче — знахідки не вичерпуються; новий світ
    засіває дно довкола спавну сам).
  - **Пірнання** — `ЛКМ` по устриці на запасі повітря кладе її в торбу
    (новий лічильник 🦪, до 16): перша здобич підказує дорогу до багаття.
    Якщо воду злили відром чи дно вибили — устриця спадає хмаркою скалок,
    як гриб без ґрунту.
  - **Пара на камені** — `ПКМ` по багатті кладе устрицю на **пласкі камені
    біля вогню** — окрему «конфорку», що працює паралельно з рожном
    (смаженина й мушля готуються водночас; з м'ясом у торбі перший `ПКМ`
    насаджує рожен, другий — кладе мушлю). За ~7 с пари з білими клубочками
    мушля **розкривається**: гарантоване **м'ясо молюска 🍤** (нова ситна
    страва на 4 «ніжки»; `F`, як завжди, сам обирає наїдок без марнування)
    і **з шансом 1 із 3 — перлина ⚪** (після трьох порожніх мушель поспіль
    перлина гарантована — смуга невдач не затягнеться). Розібране посеред
    пари багаття вертає устрицю в торбу сирою.
  - **Перлина — найдорожчий крам** — ятка торговця знає їй ціну: у пулі
    пропозицій тепер **1 ⚪ → золоте яблуко 🍏** (найкоротший шлях до
    головного призу) та **1 ⚪ → 3 меду 🍯**; рядок торби в ятці показує
    й перлини. Лічильники 🦪 🍤 ⚪ зберігаються зі світом, як і устриці на
    дні та мушля на камені (старі сейви сумісні — їхнє дно засіється само).
  - Два нові досягнення — «🦪 Пірнач» (дістати устрицю з дна) і
    «⚪ Ловець перлин» (знайти перлину в розпареній мушлі); тепер **73**
    здобутки, мета-«Майстер MineClone» враховує обидва.
  - Дебаг: `MCDebug.giveOysters(n)`, `givePearls(n)`, `sproutOysters(tries)`,
    `tpToSea()`, `tpToOyster()`, `pickNearestOyster()`, `steamOyster(fast)`,
    `oysterState()`, `oysterInfo`.

## 2026-08-09

### Додано
- **Гак-кішка з павутини: вертикаль нарешті долається пострілом, а не
  драбиною поблок.** Розглядалися три ідеї (залізні шипи-частокіл, що ранять
  нечисть при штурмі; криниця з поливом грядок; гак-кішка на павутинній
  мотузці) — обрано гак: шипи — пасивна шкода після встановлення без нової
  дії гравця (тим аргументом уже відхиляли обладунки), та й оборона
  найбагатша система гри (паркани, двері, вовки, сніговики, щит), а бойова
  дуга щойно три релізи поспіль отримувала увагу; полив — обмеження, а не
  можливість: посіви вже ростуть самі, а бджоли й борошно вже прискорюють
  їх — третій прискорювач дублює наявні. Гак же дає нове дієслово гравця —
  **постріл-підтяжку** — і закриває найстарішу діру пересування: драбина
  будується лише поблок і знизу, стрімчак, ущелина чи дах вежі досі коштували
  стовпа з блоків. Заразом павутина дістає другий ринок збуту (як руда з
  мечем услід за киркою): нічне полювання на павуків живить тепер і
  дослідження. Нуль зовнішніх ассетів (гачок, мотузка, іконка, звуки —
  процедурні):
  - **Гак-кішка** — новий предмет (Tab): `ПКМ` вистрілює гачок на павутинній
    мотузці в блок перед прицілом (до **24 блоків**; постріл коштує **1 🕸**,
    промах — нічого). Якір — вільна клітинка впритул до влученої грані, тож
    гравця ніколи не тягне всередину стіни. Без павутини гак підкаже, де її
    беруть. Працює й на сенсорних пристроях кнопкою ▦.
  - **Підтяжка** — зачепившись (цок металу об камінь), мотузка натягується і
    **тягне гравця по прямій до якоря** (13 бл/с — швидше за спринт): понад
    прірвою, лавовим озером чи вгору стрімчаком. Власний крок і гравітація
    на час підтяжки мовчать; при прибутті легкий **підскок** закидає на
    виступ.
  - **Відпускання й ризик** — `Space` чи повторний `ПКМ` відпускають мотузку
    на льоту (падіння від місця відпускання — звичайне, тож обирай момент);
    мотузка, що перестала тягти (гравець застряг об ребро чи стелю),
    **рветься сама з тріском** — павутину втрачено, як і в повідця, що
    урвався.
  - Два нові досягнення — «🪝 На гаку» (перша підтяжка) і «🧗 Верхолаз»
    (злетіти гаком на 6 блоків угору); тепер **71** здобуток,
    мета-«Майстер MineClone» враховує обидва.
  - Дебаг: `MCDebug.giveGrapple()`, `aimAt(x, y, z)`, `grappleFire()`,
    `grappleRelease()`, `grappleInfo`, `pos`.

## 2026-08-08

### Додано
- **Павутина з павуків і повідець: худобу нарешті можна привести додому.**
  Розглядалися три ідеї (гнила плоть із зомбі як корм для вовків; бос-велетень
  на вершині кривавої ночі; павутина з павуків, з якої скручується повідець) —
  обрано повідець: гнила плоть — пасивний ресурс, що дублює наявне годування
  м'ясом гіршим кормом і не дає нової дії; бос — «більше того самого бою»
  без нового дієслова гравця, та й бойова дуга щойно три релізи поспіль
  отримувала увагу (кузня, меч, щит); а повідець закриває найстарішу діру
  тваринництва — загони й розведення є, але привести знайдену в полі тварину
  до себе було неможливо (годування веде тварин одна до одної, не до гравця).
  Слизня з повідцем свого часу відхилили як «дві механіки в одній» — тепер
  повідець приходить сам, без нового моба: сировину лишає давно наявний
  павук, який досі був єдиним, окрім скелета, вартим полювання без трофея.
  Нічний бій сплітається з тваринництвом, як кістки сплели його із
  землеробством. Нуль зовнішніх ассетів (жмуток, мотузка, іконки, звуки —
  процедурні):
  - **Павутина** — здоланий павук розсипає **1–2 жмутки** (другий луут із
    нечисті після кісток): білі жмутки погойдуються на землі, підбираються
    впритул у торбу (бейдж 🕸, до 32) і зникають за 2 хв, якщо їх не забрати.
    Перший жмуток підказує скрутити повідець.
  - **Повідець** — новий предмет (Tab): `ПКМ` по свійській тварині (свиня,
    корова, вівця, курка — і дорослій, і маляті) бере її на повід за 1 🕸 —
    від грудей гравця до шиї тварини **провисає павутинна мотузка**, і
    тварина слухняно йде слідом, трохи наддавши ходи (водночас на поводі —
    до 3). `ПКМ` по тварині на поводі — відпустити (павутина вертається в
    торбу). Клік повідцем повз тварину не зайнятий — двері й хвіртки
    відчиняються як завжди.
  - **Повідець рветься** — відійти далі 12 блоків (чи сполохати тварину
    ударом так, що вона втече) — мотузка лопається з тріском, павутину
    втрачено: у горах і серед нечисті поводиря чекає обережна хода.
  - **Тварина на повідці — «своя»** — не деспавниться на відстані й
    **зберігається зі світом** (тип, позиція, маля), як приручені вовки й
    коні: доведена до дому худоба більше не зникає (старі сейви сумісні).
  - Два нові досягнення — «🕸 Павучий шовк» (підібрати павутину) і
    «🐄 Поводир» (узяти тварину на повідець); тепер **69** здобутків,
    мета-«Майстер MineClone» враховує обидва.
  - Дебаг: `MCDebug.giveSilk(n)`, `dropSilkNear(dx, dz)`, `leashNearest()`,
    `unleashAll()`, `silkCount`, `silkOnGround`, `leashInfo`; `bag` тепер
    показує і павутину.

## 2026-08-07

### Додано
- **Щит і стійка оборони: у бою нарешті є контргра, а не лише втеча.**
  Розглядалися три ідеї (щит з активним блоком; драбина луків у кузні;
  економіка трофеїв — порох із кріперів на динаміт) — обрано щит: драбина
  луків — чиста пасивна цифра шкоди без нової дії (тим аргументом уже
  відхиляли обладунки), а порохова економіка фіктивна, поки динаміт вільно
  ставиться з меню. Щит же закриває останню діру бойової дуги: атака має
  дві драбини (меч, лук), але на удар у відповідь гравець досі міг лише
  тікати чи заїдати шкоду — тепер є активна оборонна дія з таймінгом,
  найцінніша в облогу кривавої ночі. Нуль зовнішніх ассетів (модель щита,
  дзвін блоку — процедурні):
  - **Стійка оборони** — щит не предмет хотбара: тримай `C` (на сенсорі —
    нову кнопку 🛡, що з'являється зі скутим щитом) — плита підіймається
    перед груди, крок сповільнюється (×0.55), руки зайняті (ні копати,
    ні стріляти, ні ставити). Піднятий щит **відбиває фронтальні удари**
    зомбі й павуків (нуль шкоди, нападника відкидає) та **стріли скелетів**
    (дзвін металу, іскри об плиту, стріла відскакує). Напад ззаду чи збоку
    поза конусом захвату — пробиває, тож спиною до нечисті не постоїш.
  - **Третій розділ кузні «🛡 Щит»** — залізний щит (конус захвату ±60°;
    2 вугілля + 3 залізо) → алмазний (ширший конус ±80°, сильніший відкид,
    **гасить половину вибуху** кріпера чи динаміту, прийнятого на плиту;
    3 вугілля + 2 алмази). Плита фарбується під матеріал, алмазна ледь
    світиться.
  - **Збереження** — рівень щита зберігається зі світом (старі сейви
    сумісні: у них щита просто ще нема).
  - Два нові досягнення — «🛡 Щитоносець» (перший щит) і «🔰 Несхитна
    стіна» (відбитий напад); тепер **67** здобутків, мета-«Майстер
    MineClone» враховує обидва.
  - Дебаг: `MCDebug.forgeShieldNext()`, `raiseShield()`, `shieldInfo`,
    `arrowAtPlayer()`, `faceNearestMob()`, `pullNearestMob()`; `forgeInfo`
    тепер показує і щит.

## 2026-08-06

### Додано
- **Кузня кує мечі: бій нарешті має власну драбину прогресії.**
  Розглядалися три ідеї (мечі на ковадлі; обладунки з руди; система
  досвіду з рівнями) — обрано мечі: обладунки — чиста пасивна цифра без
  нової дії та видимого відгуку (тим аргументом уже відхиляли світлячків,
  музику й компас); досвід — мета-шар, що дублює вже наявні 63 досягнення
  й не має на що витрачатися; а мечі закривають найстарішу «пласку»
  величину гри — шкода удару лишалась 5 із найпершого зомбі, хоч бій
  відтоді виріс у скелетів, павуків і криваву ніч на 14 одночасної
  нечисті. Учорашнє ковадло стає повноцінною кузнею з двома драбинами, а
  руда отримує другий ринок збуту. Нуль зовнішніх ассетів (модель меча,
  свист клинка — процедурні):
  - **Меч — не предмет хотбара, а сила удару** (як кирка — швидкість
    видобутку): видобуток лишається за киркою, а на мить удару по істоті
    скутий клинок **зблискує в руці** з металевим свистом — рубальний
    рух по дузі, потім меч ховається за пояс (~1 с). Кирку в хотбарі
    нічого не витісняє.
  - **Драбина мечів у кузні** — панель ковадла тепер має два розділи:
    «⛏ Кирка» і «⚔️ Меч». Голіруч (шкода 5) → **залізний** (шкода 8;
    2 вугілля + 2 залізо) → **золотий** (шкода 11; 2 вугілля + 2 золота)
    → **алмазний** (шкода 15 — звичайний зомбі з одного удару; 3 вугілля
    + 2 алмази). Кується послідовно; сильніший меч і відкидає ворога
    вище. Клинок фарбується під матеріал, алмазний ледь світиться.
  - **Збереження** — рівень меча зберігається зі світом (старі сейви
    сумісні: у них меча просто ще нема).
  - Два нові досягнення — «⚔ Зброяр» (перший меч) і «🗡 Алмазний клинок»
    (алмазний меч); тепер **65** здобутків, мета-«Майстер MineClone»
    враховує обидва.
  - Дебаг: `MCDebug.forgeSwordNext()`, `strikeNearestMob()`; `forgeInfo`
    тепер показує і меч.

## 2026-08-05

### Додано
- **Кузня: ковадло й кування кирок — руда нарешті йде в діло.**
  Розглядалися три ідеї (кузня з рівнями кирки; світлячки й кажани в
  печерах; зілля з меду та грибів) — обрано кузню: світлячки — чиста
  атмосфера без нової дії гравця (тим аргументом уже відхиляли музику,
  блискавки, компас і сезони); зілля дублює вже багатий стіл лікувальних
  наїдків (мед, золоті яблука, смаженина — гравцеві є чим лікуватись);
  а кузня закриває найстаріший неоплачений цикл гри — видобуток: вугілля,
  залізо, золото й алмази досі платили лише досягненнями, тепер руда стає
  сировиною прогресії, що прискорює самий видобуток. Нова дія гравця —
  кування. Нуль зовнішніх ассетів (модель, іконки, дзвін молота —
  процедурні):
  - **Руда в торбі** — вибитий киркою блок руди кладе сировину в торбу
    (лічильники ⚫ вугілля до 64, ⛓ залізо до 32, 🟡 золото до 16,
    💎 алмази до 8 — один бейдж із чипами, порожні зникають); перша руда
    підказує поставити ковадло. Руда, поставлена гравцем із меню й вибита
    назад, у торбу не йде (захист від «друку» сировини з повітря).
  - **Ковадло** — новий предмет (Tab): чавунна колода з рогом на кам'яній
    підставці, ставиться `ПКМ` на тверду землю (сутність у клітинці, як
    опудало: воксельну сітку не змінює, без опори — розсипається).
    `ЛКМ` — розібрати.
  - **Кузня** — `ПКМ` по ковадлу відкриває панель із драбиною рівнів
    кирки: проста ×1 → **залізна ×1.8** (2 вугілля + 3 залізо) →
    **золота ×2.5** (3 вугілля + 3 золота) → **алмазна ×3.5** (4 вугілля
    + 3 алмази). Кується послідовно; голівка кирки в руці перефарбовується
    під матеріал, алмазна ледь світиться в темній шахті. Швидкість
    видобутку множиться на рівень кирки — алмазна гризе камінь утричі з
    половиною швидше.
  - **Збереження** — лічильники руд, рівень кирки та поставлені ковадла
    зберігаються зі світом (старі сейви сумісні).
  - Два нові досягнення — «⚒ Ковальська справа» (перше кування) і
    «🔷 Алмазний різець» (алмазна кирка); тепер **63** здобутки,
    мета-«Майстер MineClone» враховує обидва.
  - Дебаг: `MCDebug.giveOres(n)`, `anvilNear()`, `forgeNext()`,
    `openForge()`, `closeForge()`, `forgeInfo`.

### Виправлено
- **Кузня без ковадла більше не висить, а проста кирка не «перекована»
  (QA після деплою).** Прогін злитої версії показав два огріхи: на
  сенсорному екрані кнопка ⛏ дає розібрати ковадло, поки його панель
  кузні відкрита, — тепер панель у цю мить закривається; а рядок простої
  кирки після першого кування писав «уже перекована», хоч її ніхто не
  кував, — тепер «замінена міцнішою».

## 2026-08-04

### Додано
- **Мандрівний торговець і золоті яблука: надлишкам торби нарешті є куди
  дітися.** Розглядалися три ідеї (скриня-сховище; слизень у печерах, з
  якого крапає слиз на повідець для тварин; мандрівний торговець із
  бартером) — обрано торговця: скриня в грі з безлімітними блоками та
  капами лічильників — чиста зручність без власної петлі (тим аргументом
  уже відхиляли компас і музику); слизень — одразу дві механіки в одній
  (новий моб + повідець), та й печери щойно два дні поспіль отримували
  увагу (гриби і їх QA); а торговець дає економічний **стік** одразу всім
  напрацьованим петлям — полювання, курник, пасіка й грибівництво досі
  накопичували добро без застосунку (усі лічильники давно впираються в
  капи), тепер надлишки міняються на найкращий харч гри. Нова дія гравця —
  торг. Нуль зовнішніх ассетів (модель, іконка, звук — процедурні):
  - **Візит** — зранку неподалік гравця час від часу з'являється гість у
    синій хламиді з клунком за плечем (перший — невдовзі після старту
    світу, далі — раз на день-два): блукає околицями весь день, а в
    сутінках із димком іде далі своєю дорогою (тост прощання).
  - **Ятка** — `ПКМ` по торговцю відкриває панель із **трьома
    пропозиціями дня** (випадкові з пулу, щонайменше одна — із золотим
    яблуком; кожна діє тричі за візит): м'ясо 🍖, яйця 🥚, гриби 🍄 чи
    мед 🍯 → **золоте яблуко 🍏**, або гриби/яйця → **кістки 🦴** (борошно
    для тих, хто не воює зі скелетами). Поки триває торг, гість стоїть
    обличчям до покупця; внизу панелі — вміст торби.
  - **Золоте яблуко** — недоторканний запас на чорну годину (лічильник 🍏,
    до 8): `F` бере його лише коли скрутно і зі здоров'ям, і з голодом —
    або коли іншої їжі не лишилось; тоді воно **вгамовує голод ущерть і
    загоює 4 серця**.
  - **Правила світу** — торговець не деспавниться на відстані (гість, а не
    дика звірина), випавши за межі світу — повертається до гравця; удар
    змушує його панічно тікати, а вбитий він не лишає ані м'яса, ані
    здобутку «Мисливець».
  - **Збереження** — візит (позиція, пропозиції, залишки запасу), таймер
    наступного гостя та яблука 🍏 зберігаються зі світом (старі сейви
    сумісні).
  - Два нові досягнення — «🤝 Вигідна угода» (перший обмін) і «🍏 Золотий
    смак» (з'їсти золоте яблуко); тепер **61** здобуток, мета-«Майстер
    MineClone» враховує обидва.
  - Дебаг: `MCDebug.forceTrader()`, `dismissTrader()`, `traderInfo`,
    `trade(i)`, `openTrade()`, `closeTrade()`, `giveGoods(n)`,
    `giveGapple(n)`.

### Виправлено
- **Покликаний уночі торговець більше не зникає тієї ж миті (QA після
  деплою).** Прогін розгорнутої версії показав: `MCDebug.forceTrader()`
  серед ночі перемикає час на ранок, але `dayNightSun` перераховується
  лише наступним кадром циклу — тож `updateTrader` ще бачив «сутінки» й
  одразу проводжав щойно покликаного гостя (ятка не встигала відкритись).
  Природної появи це не стосувалося (там ранок перевіряється тим самим
  свіжим значенням), лише дебаг-виклику: тепер `forceTrader()` синхронізує
  сонце одразу, і форс-візит стабільний у будь-яку пору доби.

## 2026-08-03

### Додано
- **Гриби в печерах: надра нарешті пригощають, а не лише збагачують.**
  Розглядалися три ідеї (лис — нічний нальотчик на курник, якого відганяють
  паркан і вовк; компас, що вказує на дім; гриби, що проростають у темряві
  печер і печуться на багатті) — обрано гриби: лис повторив би щойно
  введену петлю нальоту (ворони вже зробили це для грядок, а проти лиса й
  захист готовий — паркан, тож нової дії гравцеві він не дає); компас —
  чиста зручність без власної петлі (тим самим аргументом уже відхиляли
  музику, блискавки й сезони, та й мінімапа з мапою скарбів навігацію вже
  тримають); а гриби дають підземеллю те, що риболовля дала воді, —
  їстівну винагороду за дослідження: досі печери платили тільки рудою,
  тепер шахтарський похід годує сам себе, сплітаючись одразу з трьома
  петлями (печери → торба → багаття, а кістки скелетів — ще й у грибівництво).
  Нуль зовнішніх ассетів (моделі, іконки — процедурні):
  - **Проростання** — у темряві під поверхнею (над клітинкою — кам'яне
    склепіння, поблизу — жодного смолоскипа чи багаття) неподалік гравця
    час від часу **проростає гриб**: червоний у білу цятку або брунатний,
    на світлій ніжці (до 48 у світі водночас; новий світ одразу засіває
    печери довкола спавну).
  - **Збирання** — `ЛКМ` по грибу кладе його в торбу (новий лічильник 🍄,
    до 64): перший збір підказує, що робити далі. Сирий гриб їстівний
    (`F`), але скромний — **1.5 «ніжки»** голоду (їсться, коли м'ясо
    скінчилося).
  - **Печений гриб** — `ПКМ` по багатті, коли сирого м'яса нема, насаджує
    на рожен гриб: за кілька секунд шкварчання — **печений гриб 🍢** на
    **4 «ніжки»** (свій лічильник; `F` сам обирає найдоцільнішу страву, як
    і зі смажениною). Розібране багаття повертає недопечений гриб сирим.
  - **Грибівництво** — кістяне борошно, посипане на гриб (`ПКМ`), **розсіює
    довкола 1–2 нових** (гриб сам не росте): з однієї знахідки — ціла
    темна грядка, а нічний бій зі скелетами тепер живить і печерне
    господарство.
  - **Правила світу** — гриб спадає хмаркою спор, якщо його клітинку зайняв
    блок чи зникла опора (видобуток, вибух, падіння сипкого блока, ріст
    дерева); інші сутності (смолоскип, драбина, посів тощо) у клітинку з
    грибом не ставляться.
  - **Збереження** — гриби, торба 🍄 і 🍢 та страва на рожні зберігаються
    зі світом (старі сейви сумісні — їхні печери засіються самі).
  - Нове досягнення «🍄 Грибник» — зібрати гриб у темній печері (тепер
    **59** здобутків; мета-«Майстер MineClone» враховує і його).
  - Дебаг: `MCDebug.giveMush(n)`, `mushroomNear(dx, dz, kind)`,
    `sproutMushrooms(tries)`, `mushState()`, `mushroomCount`, `mushBag`.

### Виправлено
- **Далекі печери більше не лишаються без грибів (QA після деплою).** Прогін
  розгорнутої версії показав: стеля у 48 грибів була **глобальною**, а гриби
  самі не зникають — за кілька хвилин гри печери довкола бази наситились
  (~40 грибів), і будь-яка печера, куди гравець спускався далі, лишалася
  голою назавжди: нових проростань нуль, знахідок нуль. Тепер стеля —
  **локальна щільність** (до 12 диких грибів у радіусі проростання), а
  глобальна межа (160) — лише запобіжник розміру збереження: коли її
  досягнуто, найдальший дикий гриб **поза очима** (далі 64 блоків) тихо
  «пересівається» — звільняє місце новому біля гравця, тож відкриття
  працюють у будь-якому кутку світу нескінченно. Вирощені борошном грядки
  позначаються окремо і **пересіванню не підлягають** (позначка
  зберігається зі світом; старі сейви сумісні).

## 2026-08-02

### Додано
- **Ворони та опудало: у врожаю з'явився ворог, а в городника — вартовий.**
  Розглядалися три ідеї (фарбування вовни барвниками з квітів; розведення
  приручених вовків і коней; ворони, що дзьобають посіви, з
  опудалом-захистом) — обрано ворон: фарби лишилися б декором без нової
  петлі (тим самим аргументом уже відхиляли музику, блискавки й сезони), та
  й квітів у світі ще немає — вийшло б дві фічі в одній; розведення
  компаньйонів повторило б щойно введену петлю приплоду без нового
  геймплею; а ворони дають землеробству те, що кривава ніч дала обороні, —
  випробування, заради якого його варто захищати, і день при цьому
  лишається мирним: гравцеві ворона не загрожує. Нуль зовнішніх ассетів
  (моделі, іконка та «кар-кар» — процедурні):
  - **Наліт** — удень за ясної погоди на грядки від **трьох підрослих
    посівів** зрідка (раз на півтори–дві з половиною хвилини) налітає
    зграйка з 2–3 ворон: чорні птахи з розмашистими крильми злітаються до
    цілі, сідають на колосся і **дзьобають його** — кожні кілька секунд
    посів відкочується на стадію росту (летять остюки). Наївшись двох
    стадій, ворона забирається геть.
  - **Ворона хитра** — її не вполювати: підійдеш ближче за 5 блоків —
    злякано зривається («кар!») і тікає; ніч, дощ і сніг теж завершують
    наліт.
  - **Опудало** — новий предмет (меню Tab): жердина з перекладиною,
    солом'яний тулуб, голова-мішок і крислатий капелюх; ставиться ПКМ на
    тверду землю й легенько похитується на вітрі. Ворони **не наважуються
    підлетіти** ближче ніж на 8 блоків — зграя шарахається й тікає. ЛКМ —
    розібрати; без опори (видобуток, вибух, падіння блока) — спадає
    соломою.
  - **Підказки** — перший клюнутий посів застерігає («Ворони дзьобають
    посіви — постав опудало!»), відлякана зграя тішить («Опудало відлякало
    ворон!»).
  - **Збереження** — опудала зберігаються зі світом (старі сейви сумісні);
    зграя тимчасова, як і решта летючих гостей.
  - Нове досягнення «🪶 Опудало на варті» — опудало відлякало ворону
    (тепер **58** здобутків; мета-«Майстер MineClone» враховує і його).
  - Дебаг: `MCDebug.giveScarecrow()`, `scarecrowNear(dx, dz)`,
    `forceCrowRaid()`, `crowState()`, `cropNear(dx, dz, stage)`,
    `cropState()`, `scarecrowCount`.

### Виправлено
- **Зграя більше не товчеться на одному колосі (QA після деплою).** Прогін
  розгорнутої версії показав: ворони нальоту нерідко обирали той самий
  посів — сідали одна в одну (моделі зливались) і обдзьобували один колос
  на дві стадії враз, лишаючи решту грядки неторканою. Тепер кожна ворона
  зграї дістає **свою** ціль, поки підрослих посівів вистачає, а при
  перенацілюванні вільні від чужого дзьоба посіви мають пріоритет — наліт
  розсипається по грядці, як і личить гайворонню.
- **Вулик тепер спадає разом з опорою на всіх шляхах оновлення світу.**
  Перевірки опори вулика бракувало після вибуху, падіння сипкого блока та
  росту дерева з саджанця — вулик міг зависнути в повітрі над зруйнованою
  опорою. Тепер `validateBeehives()` (і нова `validateScarecrows()`)
  викликаються скрізь, де й перевірки решти сутностей. Знайдено при
  інтеграції опудала.

## 2026-08-01

### Додано
- **Розведення тварин: загін за парканом нарешті став справжньою фермою.**
  Розглядалися три ідеї (мандрівний торговець; компостер, що перетворює
  надлишок їжі на кістяне борошно; розведення свійських тварин годуванням) —
  обрано розведення: торговець приніс би чужу цій грі економіку з валютою
  (усі петлі MineClone самодостатні: виростив—зібрав—згодував) і чималий
  обсяг нового інтерфейсу, компостер розмив би щойно введену петлю кісток
  (борошно перестало б бути унікальним трофеєм нічної оборони), а розведення
  сплітає землеробство, тваринництво й будівництво в одну петлю тим самим
  жестом, яким уже приручають вовків і коней — годуванням із торби 🍖.
  Нуль зовнішніх ассетів (сердечка й звуки — процедурні):
  - **Годування** — ПКМ по свині, корові, вівці чи курці з їжею в торбі 🍖
    (порція витрачається): тварина входить **«у настрій»** на пів хвилини —
    над нею зринають сердечка, і вона сама крокує до найближчої такої самої
    погодованої тварини. Підказка порадить погодувати їй пару.
  - **Приплід** — двоє «в настрої» зійшлися впритул: маля з'являється між
    батьками (феєрверк сердечок, писк, тост «У загоні приплід!») і росте на
    очах, як пташеня з яйця; батьки перепочивають півтори хвилини, перш ніж
    їх можна годувати знову.
  - **Годування маляти** — порція з торби підганяє його ріст на чверть
    хвилини: ферму можна вигодувати швидше.
  - **М'яка межа** — понад двадцятьма тваринами довкола приплоду не буде
    («Тваринам затісно») — ферма не розростеться безмежно й не з'їсть FPS.
  - **Доїння не зачеплено** — з відром у руці ПКМ по корові, як і раніше,
    доїть її; годування працює з будь-яким іншим предметом.
  - Відновлюване джерело вовни, молока, яєць і м'яса замикає петлю: посіви
    годують гравця, гравець годує загін, загін годує та вдягає гравця.
  - Нове досягнення «💞 Селекціонер» — дочекатися приплоду (тепер **57**
    здобутків; мета-«Майстер MineClone» враховує і його).
  - Дебаг: `MCDebug.spawnBreedPair(type)`, `feedInSight()`,
    `loveNearest(type)`, `breedState()`.

### Виправлено
- **Наполегливіша дорога до пари (QA після деплою).** Прогін розведення на
  дикому рельєфі показав: коли погодовані тварини стоять далеко й між ними
  горби, «настрій» міг розтанути дорогою — дві порції їжі змарновано. Тепер
  тварина, що крокує до пари, **трохи наддає ходи** (×1.3) і її настрій
  **тане вдвічі повільніше** — на рівному подвір'ї, як і раніше, пара
  сходиться за лічені секунди, а на пересіченій місцевості має вдвічі
  більше часу подолати шлях (непрохідний обрив між тваринами, як і досі,
  лишає їх без приплоду — у грі немає обходу перешкод). Дебаг-доповнення:
  `MCDebug.breedPositions()` — позиції й настрій свійських тварин.

## 2026-07-31

### Додано
- **Кістки зі скелетів і кістяне борошно: нічний бій нарешті годує ферму.**
  Розглядалися три ідеї (броня, що зменшує шкоду; світляки в нічному повітрі;
  кістки зі скелетів і кістяне борошно з них) — обрано кістки: броня розмила б
  дбайливо налаштовану загрозу кривавої ночі (оборона й так багата — паркани,
  двері, вовки, сніговики, зоряний камінь), світляки лишилися б атмосферою без
  петлі (тим самим аргументом уже відхиляли музику, блискавки, сезони та світні
  гриби), а кістки дають грі **перший луут із нечисті** та сплітають
  найрозвиненішу систему (нічний бій та облоги) із землеробством: скелети
  лишають кістки, а борошно з них прискорює посіви й саджанці. Нуль зовнішніх
  ассетів (модель кістки, іконки та звуки — процедурні):
  - **Кістки** — скелет, що загинув (від меча, стріли, вовка, сніговика чи
    догоряючи на світанку), розсипає **1–2 кістки**: маленькі білі сутності
    погойдуються на землі (як яйця), підбираються впритул у торбу (новий
    бейдж 🦴, до 64) і зникають за 2 хв, якщо їх не забрати.
  - **Кістяне борошно** — новий предмет (меню Tab): ПКМ посипає **посів**
    (підростає на цілу стадію) чи **саджанець** (стрибок ~40% росту; якщо
    дереву тісно — спробує вирости сам трохи згодом) перед прицілом —
    пилюжна хмарка із зеленими іскрами й м'який «пух». Дозрілий колос
    підкаже, що час жати; без кісток — що їх лишають скелети.
  - **Петля кривавої ночі** — облога тепер не лише випробування, а й
    винагорода: вцілілі скелети догорають на світанку й лишають кістки
    просто на подвір'ї — трофеї оборони стають добривом.
  - **Збереження** — кістки в торбі переживають перезавантаження (старі
    сейви сумісні); непідібрані кістки тлінні, як і яйця на землі.
  - Нове досягнення «🦴 Агроном» — прискорити ріст кістяним борошном
    (тепер **56** здобутків; мета-«Майстер MineClone» враховує і його).
  - Дебаг: `MCDebug.giveBones(n)`, `dropBonesNear()`, `sprinkle()`,
    `boneCount`, `bonesOnGround`.

## 2026-07-23

### Додано
- **Пасіка: вулик, бджоли та мед — фермерство отримало помічника, а
  здоров'я — перші ліки.** Розглядалися три ідеї (ведмідь — денний хижак у
  лісі; світні гриби в печерах; пасіка з вуликом і медом) — обрано пасіку:
  ведмідь зламав би навмисно мирний день (бої вже цілком належать кривавій
  ночі), гриби дублювали б смолоскипи світлом і додавали їжі там, де її
  вдосталь (атмосфера без петлі — той самий аргумент, що відхилив музику,
  блискавки й сезони), а пасіка сплітає в одну петлю одразу дві наявні
  системи — землеробство та виживання: посіви прискорюють мед, мед лікує,
  запилення прискорює посіви. Нуль зовнішніх ассетів (модель вулика, бджоли,
  іконки та гудіння — процедурні):
  - **Вулик** — новий предмет-сутність (меню Tab, за зразком багаття):
    дерев'яний будиночок із дашком і темним льотком, ставиться ПКМ на тверду
    землю, ЛКМ — розібрати. До 32 вуликів у світі.
  - **Бджоли** — удень за ясної погоди довкола вулика в'ються три бджоли зі
    смугастими тільцями й мерехтливими крильцями (дощ, сніг і ніч заганяють
    рій усередину); поблизу чутно тихе процедурне гудіння.
  - **Запилення** — посіви в радіусі 6 блоків від вулика з активними
    бджолами ростуть у **1.5 раза швидше**.
  - **Мед** — бджоли поволі наповнюють вулик (~75 с польотів); грядки поряд
    прискорюють до +96%. Повний вулик показує золоті краплі під льотком і
    виблискує іскрами, ПКМ — зібрати мед 🍯 (новий лічильник над торбою).
  - **Мед лікує** — перші цілющі ласощі гри: F з'їдає мед передусім, коли
    бракує здоров'я (**+3 серця** та +2 ніжки голоду) — раніше здоров'я
    поверталося лише повільною регенерацією при ситості.
  - **Помста рою** — розібраний вулик, у якому вже чимало меду, коштує
    жала: 2 од. шкоди й нова причина смерті («Зажалений розлюченими
    бджолами»).
  - **Збереження** — вулики, мед у них і мед у торбі переживають
    перезавантаження; старі сейви сумісні.
  - Нове досягнення «🍯 Бортник» (тепер **55** здобутків; мета-«Майстер
    MineClone» враховує і його).
  - Дебаг: `MCDebug.placeHive()`, `hiveState()`, `forceHoney()`,
    `harvestHive()`, `eatHoney()`, `hurt()`, `honeyCount`.

## 2026-07-22

### Додано
- **Скарбна мапа з пляшки: риболовля, що інколи дарує пригоду.** Розглядалися
  три ідеї (фантом, що карає безсонних; сезони зі зміною барв світу; мапа
  скарбів із виловленої пляшки) — обрано мапу скарбів: фантом суперечив би
  кривавій ночі (вона сама *забороняє* спати — карати за те саме двічі
  нечесно), сезони лишилися б атмосферою без нової петлі (тим самим
  аргументом уже відхиляли музику й блискавки), а мапа нарешті дає вудці
  щось, крім їжі, мінімапі — квест, якому вона служить, а закопування
  через звичайні воксельні правки обходить заперечення «зміни генерації
  не дістануться вже дослідженим околицям», яким свого часу відхилили
  підземні скарбниці. Нуль зовнішніх ассетів (текстура скрині, маркер і
  звуки — процедурні):
  - **Пляшка з мапою** — зрідка (шанс 18%) замість риби клює пляшка:
    повідомлення підказує напрямок і відстань («🍾 У пляшці — мапа скарбів!
    ✕ на мінімапі: ПдЗх, ~132 м»), а у 60–140 блоках від гравця на суходолі
    (не в стовбурі дерева й не в кактусі) на пару блоків під поверхнею
    закопується скриня скарбів. Одночасно живе лише одна мапа.
  - **Золотий ✕ на мінімапі** — маркер схованки з відстанню в метрах під
    міткою «Пн»; поза радіусом огляду ✕ притискається до краю кільця і
    вказує напрямок, тож мандрівник ніколи не губить курс.
  - **Скриня скарбів** — новий блок: дубові дошки з темною окантовкою,
    двома золотими обручами та замком. Розбита скриня-схованка дає **повне
    відновлення здоров'я та припасів**, золотий феєрверк і досягнення
    «🪙 Шукач скарбів» (тепер **54** здобутки; мета-«Майстер MineClone»
    враховує і його). Скриня є і в меню (Tab) як декоративний блок — але
    нагороду дає лише та, до якої вела мапа.
  - **Чесність мапи** — знищена вибухом (динаміт, кріпер, метеорит) скриня
    «гасить» мапу з повідомленням, щоб ✕ не вказував вічно на порожнє місце.
  - **Збереження** — активна мапа й закопана скриня переживають
    перезавантаження (скриня — звичайна воксельна правка); старі сейви
    сумісні.
  - Дебаг: `MCDebug.forceBottle()`, `digTreasure()`, `treasureInfo`.

### Виправлено
- **README наздогнав гру.** Лічильник досягнень у README застряг на 52
  (без «Зоряного ловця» з релізу зорепаду) — тепер 54, з описом нової
  скарбної мапи та згадками зоряних здобутків.

## 2026-07-21

### Додано
- **Зорепад і впала зоря: небо, що інколи дарує скарб.** Розглядалися три
  ідеї (мандрівний торговець; підземні скарбниці в надрах; зорепад із
  метеоритом, що падає у світ) — обрано зорепад: торговцю нема чим платити
  (у грі немає валюти чи обмеженого інвентаря, обмін був би бутафорією),
  скарбниці вимагали б змін генерації, які ніколи не дістануться вже
  дослідженим околицям, а падаюча зоря додає нічному небу видовища й
  дає нову петлю «помітив — біжи — забери» до вже наявних добування та
  оборони. Нуль зовнішніх ассетів (небо, вогонь і камінь — процедурні):
  - **Зорепад** — глибокої ночі небосхилом зрідка черкають падаючі зорі:
    яскрава голова з в'янучим шлейфом, що гасне за мить (суто окраса,
    видима лише вночі).
  - **Метеорит** — деякі ночі (шанс 45%) одна зоря летить у світ
    по-справжньому: з попередженням «🌠 З неба летить зоря!» розжарена куля
    з вогняним слідом і димом падає за 25–50 блоків від гравця, вибухає
    (кратер, як від динаміту; впритул — боляче) і вкраплює в дно кратера
    **3–6 блоків зоряного каменю**.
  - **Зоряний камінь** — новий блок: темне індиго з крижаними «зорями» та
    золотими іскрами. **Світиться** холодним сяйвом (пул ламп, як у лави)
    і **відлякує нечисть далі за смолоскип** (радіус 9 проти 7; навіть
    кривавої ночі тримає монстрів на 4.5 блока проти 3.5 у вогню). Твердий
    (добувається довше за алмаз), ставиться з меню (Tab) як звичайний блок.
  - **Досягнення** — «🌠 Зоряний ловець» за добутий уламок впалої зорі
    (тепер **53** здобутки; мета-«Майстер MineClone» враховує і його).
  - **Збереження** — вкраплені блоки лишаються у світі назавжди (звичайні
    правки вокселів), реєстр сяйва відновлюється із сейва; старі сейви
    сумісні.
  - Дебаг: `MCDebug.forceMeteor()`, `shootingStar()`, `starInfo`.

## 2026-07-20

### Додано
- **Кривава ніч: облога, що перевіряє твою оборону.** Розглядалися три ідеї
  (процедурна фонова музика день/ніч; бджоли, вулики та мед; кривава ніч —
  періодична облога нечисті) — обрано криваву ніч: музика лишилася б
  атмосферою без нової петлі (тим самим аргументом уже відхиляли блискавки),
  мед — «ще одна їжа» одразу після смаженини й молока, а облога нарешті дає
  **застосунок усій накопиченій обороні**: паркани, двері, смолоскипи,
  приручені вовки та зліплені сніговики досі захищали від млявого нічного
  спавну — тепер є ніч, заради якої їх варто будувати. Нуль зовнішніх
  ассетів (небо, звук і поведінка — процедурні):
  - **Жереб сутінків** — щойно западає темрява, ніч може стати кривавою
    (не раніше 3-ї ночі; шанс 25%, але після 4 спокійних ночей поспіль —
    гарантовано). Небо, хмари й місяць плавно **багряніють**, лунає низький
    дисонансний гул і застереження «🌘 Кривава ніч!».
  - **Облога** — нечисть спавниться **втричі частіше** (кожну секунду
    замість трьох) і аж до **14 одночасно** (замість 6); зомбі, кріпери,
    скелети й павуки, що вилазять цієї ночі, **на 15% швидші та на 4 серця
    міцніші**. Світло смолоскипів і жар багать відлякують їх лише
    **впритул** (радіус 3.5 блока замість 7) — самою ілюмінацією базу вже
    не вбережеш.
  - **Ліжко не рятує** — заснути кривавої ночі неможливо («тримай
    оборону!»): облогу треба саме пережити. На світанку багрянець тане,
    вцілілі зомбі й скелети догорають на сонці, а гравцеві, що **не загинув
    жодного разу** від сутінків до світанку, — нове досягнення
    «🌘 Багряна варта» (тепер **52** здобутки; мета-«Майстер MineClone»
    враховує і його).
  - **Збереження** — лічильник ночей, ритм облог і сама активна кривава ніч
    зберігаються зі світом (перервана сейвом облога продовжується після
    завантаження); старі сейви сумісні.
  - Дебаг: `MCDebug.forceBloodNight()`, `endBloodNight()`, `bloodInfo`.

### Виправлено
- **Багрянець глибокої ночі (QA після деплою).** На піку облоги небо
  читалося радше темно-фіолетовим, ніж червоним (багряний тон губився на
  тлі нічної синяви) — червоний компонент тону підсилено (`0x2a0507` →
  `0x400a0c`), а частку домішування збільшено з 0.6 до 0.65: тепер небо
  кривавої ночі впізнавано винно-червоне і опівночі, не лише в сутінках.

## 2026-07-19

### Додано
- **Сніговик-охоронець: зліпи собі нічну варту.** Розглядалися три ідеї
  (справжні удари блискавки в грозу; їстівні гриби-світляки в печерах;
  сніговик-охоронець зі сніжками) — обрано сніговика: блискавка лишилася б
  здебільшого косметикою (спалах і грім уже є), гриби — «ще одна їжа» одразу
  після багаття, а сніговик додає справді нову петлю — **збудованого
  союзника**: не прирученого, а зліпленого власноруч із блоків. Фіча сплітає
  будівництво, снарядну систему яйця, бойову охорону вовка та нічні облоги.
  Нуль зовнішніх ассетів (модель, іконка та звуки — процедурні):
  - **Зліплення** — постав колону **сніг + сніг + гравій** (гравієва «голова»
    зверху): блоки зникають, і на їхньому місці **оживає сніговик** (хмарка
    снігу й висхідний дзвінкий мотив) — три снігові грудки, очі-вуглинки,
    ніс-морквина, руки-гілки та гравієва «шапка». Працює й тоді, коли гравій
    **сам падає** (гравітація сипких блоків) на готову снігову колону.
  - **Варта** — сніговик тупцює біля місця, де його зліпили (не блукає далі
    ~4 блоків, а відійшовши — вертається), і **обстрілює сніжками** нечисть
    у радіусі 12 блоків, маючи лінію зору: дуговий приціл із компенсацією
    падіння, кидок кожні ~1.6–2.2 с, 1 шкода + **відкид**, що збиває натиск
    зомбі й павуків. Нейтрального денного павука не чіпає (не варто злити).
    Свого сніговика не поранити ні ударом, ні стрілою, ні сніжкою; гине він
    від лави та вибухів — тане димком, як і личить снігові.
  - **Сніжка** — новий предмет (меню `Tab`): безлімітний метальний снаряд і
    для гравця — `ПКМ` кидає дугою (працює й на сенсорних кнопкою ▦), сніжка
    розсипається об блок, двері, паркан чи істоту м'яким «пухом» частинок;
    нечисть дістає 1 шкоди з відкидом, тварин сніжка **не ранить**.
  - **Збереження** — сніговики (з місцем варти) зберігаються зі світом;
    старі сейви сумісні.
  - **Нове досягнення** «⛄ Снігова варта» — зліпити сніговика (тепер **51**
    здобуток; мета-«Майстер MineClone» враховує і його). Дебаг:
    `MCDebug.giveSnowball()`, `throwSnowball()`, `buildGolemNear(dx, dz)`,
    `golems`, `snowballsFlying`.

## 2026-07-18

### Додано
- **Багаття: тепер здобич можна засмажити.** Розглядалися три ідеї
  (скриня-сховище; ендермен-телепортер; багаття з приготуванням їжі) —
  обрано багаття: скриня мало що дає при безлімітному хотбарі, ще один
  нічний моб одразу після павука не додав би нової петлі, а кухня сплітає
  одразу кілька перевірених систем (полювання, риболовлю, голод, світло й
  частинки) у новий цикл «вполюй → засмаж → наїжся краще». Нуль зовнішніх
  ассетів (модель, іконки та звуки — процедурні):
  - **Багаття** — новий предмет (меню `Tab`): ставиться `ПКМ` на тверду
    землю (сутність у клітинці, як смолоскип — воксельну сітку не змінює).
    Кам'яне коло, перехрещені колоди, рожен і живе полум'я, що мерехтить,
    димить і пускає іскри; вночі **світить** власним пулом динамічних ламп
    і **відлякує нечисть** у радіусі 7 блоків, як смолоскип. `ЛКМ` —
    розібрати; без опори знизу чи коли клітинку займає блок — спадає.
  - **Смаження** — `ПКМ` по багатті з сирим м'ясом 🍖 у торбі насаджує
    порцію на рожен: вона шкварчить, димить і поволі підрум'янюється, а за
    ~6 с дзвінке «дзинь» — і в торбі **смаженина 🍗** (новий бейдж поряд із
    сирим м'ясом). Смаженина відновлює **5 «ніжок»** голоду замість 3 у
    сирого; `F` сам обирає, що з'їсти, аби не змарнувати жодної «ніжки».
    Розібрати багаття посеред смаження — недосмажена порція повернеться
    сирим м'ясом.
  - **Опік** — стати в полум'я боляче: гравець загоряється (нова причина
    смерті «Згорів у багатті»), вогонь догорає й поза вогнищем; молоко, як
    і від лави, гасить.
  - **Збереження** — багаття (разом із порцією, що смажиться) і запас
    смаженини зберігаються зі світом; старі сейви сумісні.
  - **Нове досягнення** «🍗 Кухар» — засмажити м'ясо (тепер **50**
    здобутків; мета-«Майстер MineClone» враховує і його). Дебаг:
    `MCDebug.giveCampfire()`, `campfireNear()`, `giveFood(n)`, `cookMeat()`,
    `cookFast()`, `eat()`, `setHunger(h)`, `campfires`, `bag`.

## 2026-07-17

### Додано
- **Рейки та вагонетки: колія для швидких подорожей суходолом.** Розглядалися
  три ідеї (рейки з вагонеткою; скриня-сховище; ендермен-телепортер) — обрано
  вагонетки: вони спираються на перевірену інфраструктуру «сідел» (човен,
  кінь) і додають нову петлю геймплею — **будування транспортної мережі**, а
  не лише ще одну істоту. Нуль зовнішніх ассетів (моделі, іконки й звуки —
  процедурні):
  - **Рейки** — новий предмет (меню `Tab`): кладуться `ПКМ` на тверду
    поверхню (сутність у клітинці, як драбина — воксельну сітку не змінює) і
    **самі з'єднуються** із сусідніми рейками у прямі ділянки та **повороти
    на 90°**; «висячий» кінець сусідньої рейки розвертається назустріч
    покладеній. Модель — дві сталеві рейки на дерев'яних шпалах (поворот —
    діагональна хорда). `ЛКМ` знімає рейку; без твердої опори знизу або коли
    клітинку займає блок (вибух, пісок, що впав) — рейка спадає.
  - **Вагонетка** — новий предмет: ставиться `ПКМ` на рейку в прицілі
    (залізний короб на колесах), `ПКМ` по вагонетці — **сісти** (як у човен).
    `W` — розганятися вздовж колії (до **9 бл/с** — швидше за спринт), `S` —
    гальмувати й **задкувати**, `Space` — злізти. Рух іде графом центрів
    клітинок: на поворотах вагонетка **сама повертає** (модель плавно
    довертається), у кінці колії — зупиняється, тертя кочення поступово
    гасить швидкість вільної вагонетки. Вагонетка без рейки **падає** з
    гравітацією і, приземлившись на колію, знову стає на неї; `ЛКМ` — зняти.
  - **Сумісність «сідел»** — човен, кінь і вагонетка взаємовиключні (з одного
    в інше на льоту не пересісти); при загибелі гравець випадає і з
    вагонетки. Вершник не дістає шкоди від падіння; на сенсорних пристроях
    тяга — джойстиком, злізти — кнопкою ⬆.
  - **Збереження** — рейки (з орієнтацією) і вагонетки зберігаються зі
    світом; старі сейви сумісні.
  - **Нове досягнення** «🛤 Машиніст» — розігнатися вагонеткою (тепер **49**
    здобутків; мета-«Майстер MineClone» враховує і його). Дебаг:
    `MCDebug.giveRail()`, `giveMinecart()`, `railLineNear(len)`,
    `railLoopNear(w,h)`, `cartOnRail()`, `cartPush(v)`, `mountNearCart()`,
    `rails`, `carts`.

## 2026-07-16

### Додано
- **Павуки: нічні верхолази, від яких не сховатися за муром.** Розглядалися
  три ідеї (павук, що лазить стінами; їстівні гриби-світляки в печерах;
  скриня-сховище) — обрано павука: він двічі поступався в попередніх
  брейнштормах, а нічний бій — найрозвиненіша система гри, якій бракувало
  відповіді на тактику «відсидітися на стовпі». Нуль зовнішніх ассетів
  (модель із боксів, сичання — синтезоване):
  - **Модель і спавн** — присадкуватий восьминогий силует (голова, груди,
    черевце, розчепірені ноги, що дріботять у шаховому порядку) з **червоними
    очима, які жевріють у пітьмі** (не гаснуть від спалаху болю). З'являється
    вночі поряд з рештою нечисті (≈чверть спавнів); світло смолоскипа
    відлякує, як і всіх.
  - **Лазіння стінами** — головна загроза: упершись у перешкоду, павук
    **чіпляється й повзе вертикально вгору** (3.2 бл/с), тож мур, стовп чи
    вежа більше не рятують — тільки дах, рів або бій. Перевірено QA: павук
    долає кільце зі стін заввишки 4 блоки.
  - **Стрибок** — зблизька (1.6–3.6 блока) павук **стрибає** на гравця з
    різким сичанням (нове `Sound.spiderHiss`); кусає при контакті (2 шкоди
    + відкид, новий екран смерті «Ужалений павуком»). Швидший за зомбі
    (3.0 бл/с проти 2.2), але кволіший (10 здоров'я).
  - **Нейтральний удень** — не згоряє на світанку (як кріпер), а на світлі
    **мирно блукає** й не чіпає гравця, доки той не вдарить першим — тоді
    лютішає й нападає незалежно від доби. Вовк-охоронець сам атакує павуків
    поряд із хазяїном.
  - **Нове досягнення** «🕷 Арахнофобія» — здолати павука (тепер **48**
    здобутків; мета-«Майстер MineClone» враховує і його). Дебаг:
    `MCDebug.spawnSpider(n, angry)`, `spiders`.

## 2026-07-15

### Додано
- **Корів тепер можна доїти, а молоко — пити.** Розглядалися три ідеї
  (павук-верхолаз, що лазить стінами; їстівні гриби в печерах; молоко з
  корів) — обрано молоко як найглибшу синергію з перевіреними системами:
  відро вже має стани (вода/лава), корови давно пасуться без діла, а голод
  і горіння від лави чекали на протиотруту. Нуль зовнішніх ассетів (іконка
  відра з молоком, цвіркання доїння та ковтки — процедурні):
  - **Доїння** — навестися на корову з **порожнім відром** (`ПКМ`, на
    сенсорних — кнопка ▦): цвіркання струменя, білі бризки — і в руці
    «Відро з молоком». Одна корова надоюється знову за ~30 с; спроба
    подоїти зарано — підказка «Корова ще не надоїлась» (і відро не хапає
    флюїд позаду корови).
  - **Пиття** — `ПКМ` з відром молока: три булькотливі ковтки відновлюють
    3 «ніжки» голоду і **гасять полум'я** на гравцеві (нове рятівне коло
    після зустрічі з лавою); відро повертається порожнім. Ситому й не
    палаючому — підказка «Ситий — молоко зачекає», щоб не змарнувати
    надоєне.
  - **Нове досягнення** «🥛 Молочар» — подоїти корову (тепер **47**
    здобутків; мета-«Майстер MineClone» враховує і його). Дебаг:
    `MCDebug.cowNear()`, `milkCow()`, `drinkMilk()`, `giveBucket('milk')`,
    `cows`.

### Виправлено
- **Повне відро більше не губиться між сесіями.** Відновлення хотбара з
  сейва пропускало лише предмети з меню (`Tab`), а станів відра (вода /
  лава, тепер і молоко) там немає — тож набране відро після перезапуску
  ставало «дефолтним» блоком. Тепер стани відра явно дозволені в хотбарі.
  Знайдено під час інтеграції відра з молоком.

## 2026-07-14

### Виправлено
- **Бейджі 🥚/🍖 більше не лягають на сенсорні кнопки.** На сенсорних
  пристроях лічильники яєць та їжі (правий нижній кут) перекривали стовпчик
  кнопок керування (⬆⛏▦🍖▤) — новий бейдж яєць сідав просто на кнопку меню.
  Тепер на пристроях із грубим вказівником (`pointer: coarse`) бейджі
  зсуваються лівіше кнопок. Знайдено при QA розгорнутої версії в емуляції
  телефона.
- **Бейдж їжі 🍖 більше не висить на екрані з нулем.** Авторське
  `display: flex` перекривало атрибут `hidden`, тож порожній лічильник м'яса
  було видно завжди. Додано явне правило `[hidden] { display: none }` для
  бейджів їжі та яєць. Знайдено при QA нової яєчної механіки.

### Додано
- **Таблички: світ нарешті можна підписувати.** Розглядалися три ідеї
  (кінь верхи, таблички з написами, яйця з курчатами) — перші дві вже
  встигли з'явитися паралельно, тож обрано таблички: анотації для світу
  (підписати ферму, склад, дороговказ) на перевіреному патерні ліжка.
  Нуль зовнішніх ассетів (модель, текстура напису й іконка — процедурні):
  - **Встановлення** — новий предмет «Табличка» (`SIGN`, меню `Tab`):
    `ПКМ` ставить дерев'яний щит на стовпчику на тверду опору **лицем до
    гравця** (крок 45°) і відкриває **редактор напису** — охайна панель
    із текстовим полем (до 64 символів), `Enter` зберігає, `Esc` скасовує
    (працює й на сенсорних пристроях з віртуальною клавіатурою). Курсор
    на час вводу звільняється, клавіші редактора не «протікають» у гру.
  - **Напис** — малюється на canvas-текстурі дошки: до 4 рядків по ~13
    символів із перенесенням по словах, рамка та «шви» дощечок. `ПКМ` по
    готовій табличці — **редагувати** напис у тому ж редакторі.
  - **Взаємодія зі світом** — `ЛКМ` знімає табличку (тріски + дерев'яний
    звук); вона спадає, якщо зруйнована опора під нею (видобуток, вибух,
    падіння блока) чи її клітинку зайняв блок. Клітинки табличок
    взаємовиключні зі смолоскипами, посівами, ліжками, драбинами, дверима,
    огорожею та саджанцями.
  - **Збереження** — таблички (`[x, y, z, yaw, text]`) зберігаються зі
    світом; старі сейви сумісні.
  - **Нове досягнення** «🪧 Літописець» — написати табличку (тепер **46**
    здобутків; мета-«Майстер MineClone» враховує і його). Дебаг:
    `MCDebug.giveSign()`, `placeSignNear(text, dx, dz)`, `editNearestSign()`,
    `confirmSignEditor(text)`, `cancelSignEditor()`, `signEditorOpen`,
    `signs`.
- **Кури несуть яйця, а з кинутих яєць вилуплюються курчата.** Розглядалися
  три ідеї (павук-верхолаз, скриня-сховище, яйця з курчатами) — обрано яйця
  як найкращу петлю геймплею з опертям на перевірені системи (курка, снаряди
  лука, торба їжі, загони з парканів). Нуль зовнішніх ассетів (модель яйця,
  іконка, кудкудакання, писк і хрускіт шкаралупи — синтезовані):
  - **Кладка** — доросла курка кожні ~25–50 с (стоячи на землі, не в паніці)
    несе яйце: кудкудакання, пір'яна хмарка, і маленьке яйце лишається на
    землі, погойдуючись. Непідібране яйце зникає за 2 хв.
  - **Підбирання** — підійти впритул: яйце злітає в торбу (бейдж 🥚 над
    бейджем їжі 🍖, до 64 шт.), лічильник зберігається зі світом.
  - **Кидання** — предмет «Яйце» (меню `Tab`), `ПКМ` кидає дугою, як снаряд
    (працює й на сенсорних пристроях кнопкою ▦): яйце розбивається об блок,
    двері, паркан чи істоту бризками шкаралупи та жовтка. Нечисть дістає
    1 шкоди й легкий відкид; тварин яйце не ранить (власних курей у загоні
    не покалічити). Без яєць у торбі — підказка над хотбаром.
  - **Вилуплення** — з розбитого яйця з шансом 1 із 3 вилуплюється **курча**:
    зменшене пташеня попискує, блукає поряд і за ~60 с плавно виростає в
    дорослу курку, яка сама почне нести яйця. Загін за парканом —
    постійна яєчна ферма.
  - **Два нові досягнення** — «🥚 Курочка ряба» (підібрати яйце) і «🐣 Нове
    життя» (вилупити курча); тепер **45** здобутків, мета-«Майстер MineClone»
    враховує обидва. Дебаг: `MCDebug.spawnChicken()`, `layEgg()`,
    `giveEggs()`, `throwEgg()`, `eggState`.

## 2026-07-13

### Виправлено
- **Сідло і човен більше не діляться вершником.** `ПКМ` по човну верхи на
  коні сідав у човен, не спішивши гравця з коня — обидва «сідла» тягли його
  одночасно. Тепер сісти в човен можна лише спішившись (симетрично до коня,
  на якого не сісти з човна). Знайдено при QA розгорнутої версії.

### Додано
- **Коні та їзда верхи: швидка подорож суходолом.** Нова тварина «Кінь» —
  за перевіреними патернами вовка (приручення) і човна (керування), нуль
  зовнішніх ассетів (модель з боксів, іржання — синтезоване):
  - **Дикі коні** пасуться на відкритих рівнинах (біом `PLAINS`, ~22% спавнів)
    поряд зі свійськими тваринами: блукають, перестрибують перешкоди,
    плавають, панікують від удару.
  - **Приручення** — `ПКМ` з їжею 🍖 у торбі годує коня (сердечка): кожна
    порція — шанс приручити, третя — напевно. На прирученому з'являється
    **сідло з попоною**; він пасеться на місці, чекає вершника, не деспавниться
    й лікується годуванням.
  - **Їзда верхи** — `ПКМ` по осідланому коню — сісти в сідло: кермо поглядом
    (`W` — уперед, `S` — назад, на сенсорі — джойстик), **галоп швидший за
    біг гравця** (9.5 бл/с проти 8), автострибок через блок заввишки на ходу,
    убрід і вплав через річки (повільно). `Space` (кнопка ⬆ на сенсорі) —
    злізти. Вершник не дістає шкоди від падіння; лава палить коня й під
    сідлом; загине кінь — вершник просто спішується.
  - **Безпека взаємодій** — власного коня під сідлом не вдарити (`ЛКМ` б'є
    повз), сон у ліжку спішує, смерть і посадка в човен несумісні з сідлом.
  - **Збереження** — приручені коні (`[x, y, z, health]`) зберігаються зі
    світом; старі сейви сумісні.
  - **Нове досягнення** «🐴 Вершник» — приручити коня й сісти верхи (тепер
    **43** здобутки; мета-«Майстер MineClone» враховує і його). Дебаг:
    `MCDebug.spawnHorse()`, `feedHorse()`, `mountHorse()`, `dismountHorse()`,
    `horseState()`, `horses`, `ridingHorse`.

## 2026-07-11

### Додано
- **Саджанці: відновлюваний ліс.** Новий предмет «Саджанець» (`SAPLING`) —
  сутність за перевіреним патерном посівів (не воксель), нуль зовнішніх
  ассетів:
  - **Посадка** — `ПКМ` на траву чи землю садить паросток (процедурна
    текстура-хрест: стовбурець і зелена крона); клітинки взаємовиключні зі
    смолоскипами, посівами, ліжками, драбинами, дверима й огорожею. `ЛКМ`
    знімає паросток; він спадає, якщо зруйнована опора під ним (видобуток,
    вибух, падіння блока) чи його клітинку зайняв блок.
  - **Ріст** — живиться світлом, як посіви: швидше під сонцем, повільніше в
    негоду, вночі трохи допомагає смолоскип поряд; паросток помітно
    **тягнеться вгору** в міру росту й погойдується на «вітрі». Назбиравши
    зросту (~50 с повного сонця), виростає у **справжнє воксельне дерево** —
    стовбур із колод (4–5) і крона з листя тієї самої форми, що й у
    згенерованих дерев (листя — лише у вільні клітинки).
  - **У тісняві не росте** — стовбуру потрібні вільні клітинки (і не можна
    рости крізь гравця): саджанець терпляче чекає й пробує знову за кілька
    секунд. Нові блоки дерева коректно виселяють сутності зі своїх клітинок
    (смолоскипи, сусідні саджанці тощо — спільні validate-перевірки).
  - **Збереження** — саджанці (`[x, y, z, growth]`) зберігаються зі світом;
    старі сейви сумісні.
  - **Нове досягнення** «🌳 Лісівник» — виростити дерево з саджанця (тепер
    **42** здобутки; мета-«Майстер MineClone» враховує і його). Дебаг:
    `MCDebug.giveSapling()`, `saplingNear(dx, dz)`, `growSaplings()`,
    `saplingState()`, `saplingCount`.

## 2026-07-10

### Додано
- **Паркан і хвіртка: огорожа, яку не перестрибнути.** Два нові предмети —
  «Паркан» (`FENCE`) і «Хвіртка» (`GATE`) — сутності за перевіреним патерном
  дверей (не вокселі), нуль зовнішніх ассетів:
  - **Паркан** — секція зі стовпчиком, що ставиться `ПКМ` на тверду опору;
    сусідні секції **з'єднуються перекладинами** між собою, з хвіртками та
    твердими блоками (плечі оновлюються автоматично при будь-якій зміні
    довкола). Для колізій огорожа **«заввишки півтора блока»**: блокує і
    клітинку над собою через спільний `moveEntityAxis`, тож гравець, тварини
    й нечисть **не перестрибнуть** — нарешті можна будувати **загони для
    овець**, вольєри для вовків і безпечні двори, які видно наскрізь.
  - **Хвіртка** — стулка на завісах між двома стовпчиками, ставиться лицем до
    гравця. `ПКМ` (з будь-яким предметом) **відчиняє/зачиняє** з плавним
    обертом і рипом (процедурний звук дверей); зачинити стулку на гравцеві,
    тварині чи нечисті не вийде (перевірка перетину AABB). Зачинена хвіртка —
    така сама «висока» перешкода, як паркан; відчинена — вільний прохід.
  - **Взаємодія зі світом** — стріли **встромляються** в паркан і зачинену
    хвіртку; `ЛКМ` знімає секцію (тріски + дерев'яний звук); огорожа спадає,
    якщо зруйнована опора під нею (видобуток, вибух, падіння блока) чи її
    клітинку зайняв блок. Клітинки огорожі взаємовиключні зі смолоскипами,
    посівами, ліжками, драбинами й дверима.
  - **Збереження** — паркани (`[x, y, z]`) і хвіртки (`[x, y, z, dx, dz,
    open]`) зберігаються зі світом; старі сейви сумісні.
  - **Нове досягнення** «🚧 Обгороджено» — поставити паркан чи хвіртку (тепер
    **41** здобуток; мета-«Майстер MineClone» враховує і його). Дебаг:
    `MCDebug.giveFence()`, `giveGate()`, `fenceNear(dx, dz)`, `gateNear()`,
    `toggleNearGate()`, `gateState()`, `fenceBlocked(x, y, z)`, `fences`,
    `gates`.

### Виправлено
- **README** — додано пропущений опис вовків (з релізу 2026-07-09) і оновлено
  застарілий лічильник досягнень (39 → 41).

## 2026-07-09

### Додано
- **Вовк: приручення та вірний компаньйон.** Нова тварина «вовк» (`wolf`) за
  перевіреним патерном тварин, що вперше дає гравцеві союзника в бою — нуль
  зовнішніх ассетів:
  - **Дикі вовки** — сіра модель (світла голова, темні вуха й хвіст) водиться
    в **лісі й тундрі** (у сніговій тундрі — єдина тварина, бо решта пасеться
    лише на траві); блукає, перестрибує перешкоди, плаває й панікує від удару,
    як інші тварини.
  - **Приручення** — `ПКМ` по вовку з м'ясом 🍖 у торбі годує його порцією:
    хмарка сердечок і скавуління; кожна порція — шанс приручити (третя —
    напевно). Приручений вовк отримує **червоний нашийник** і радісно
    **метляє хвостом**.
  - **Вірний друг** — приручений вовк **іде за гравцем** (доганяє бігом, як
    відстане), а загубившись далі ~24 блоків чи випавши за межі світу —
    **телепортується** до хазяїна. Удари й стріли гравця **не ранять** свого
    вовка.
  - **Охоронець** — нечисть (зомбі, кріпер, скелет) у радіусі ~10 блоків від
    гравця чи вовка стає ціллю: вовк кидається бігом і **кусає** з
    випадом-стрибком і гавкотом (4 шкоди раз на ~0.9 с), тож нічна облога
    будинку тепер має відповідь. Убивства вовка зараховуються гравцеві
    (здобутки за зомбі/кріпера/скелета).
  - **Команди й догляд** — `ПКМ` по здоровому прирученому вовку — **сидіти/йти**
    (сидячи, підбирає задні лапи й нікуди не тікає); годування пораненого
    **лікує** (+5 здоров'я за порцію).
  - **Збереження** — приручені вовки зберігаються зі світом (формат
    `[x, y, z, health, sitting]`), сидячий лишається сидіти після
    перезавантаження; старі сейви сумісні (поле необов'язкове). На мінімапі
    приручений вовк — **золота крапка** (дикий — сіра).
  - **Нове досягнення** «🐺 Вірний друг» — приручити вовка (тепер **40**
    здобутків; мета-«Майстер MineClone» враховує і його). Дебаг:
    `MCDebug.spawnWolf()`, `giveMeat(n)`, `feedWolf()`, `wolfState()`,
    `toggleSitWolf()`, `wolves`, `tpPlayer(dx, dz)`.

## 2026-07-08

### Додано
- **Вівці та вовна.** Нова пасивна тварина «вівця» (`sheep`) за перевіреним
  патерном тварин (свинка/корова/курка) та новий будівельний блок «Вовна»
  (`WOOL`) — нуль зовнішніх ассетів:
  - **Вівця** — пухнаста модель (кремове руно поверх «шкіри», темні морда й
    ноги) пасеться на траві поряд з іншими тваринами, блукає, перестрибує
    перешкоди, плаває й панікує від удару.
  - **Стрижка** — перший удар (`ЛКМ` чи стріла) по нестриженій вівці
    **зістригає руно замість шкоди**: хмарка вовняних частинок, процедурний
    «вжик» ножиць, вівця лякається й тікає обстриженою (руно-меші ховаються,
    видно тіло під ними). Вовна **відростає** за ~60 с. Обстрижену вівцю можна
    полювати як звичайну тварину (сире м'ясо в торбу їжі).
  - **Блок «Вовна»** — м'який кремовий воксель (меню `Tab`): кучерява
    процедурна текстура (тайл атласу з детермінованими завитками й пухом),
    швидко добувається (0.45 с), кроки по ньому — приглушені (низькочастотний
    фільтр у синтезі звуку). На мінімапі — світло-кремовий.
  - **Нове досягнення** «🐑 Стрижій» — обстригти вівцю (тепер **39** здобутків;
    мета-«Майстер MineClone» враховує і його). Вівці позначаються на мінімапі
    білими крапками. Дебаг: `MCDebug.spawnSheep()`, `sheepState()`,
    `giveWool()`, `sheepCount`.

## 2026-07-07

### Додано
- **Двері: прохід, що відчиняється.** Новий окремий предмет «Двері» (`DOOR`) —
  будинок із ліжком, вікнами й смолоскипами нарешті має справжній вхід. Сутність
  за перевіреним патерном драбини (колона з двох клітинок, не воксельний блок) —
  нуль зовнішніх ассетів і жодних правок сітки чанків:
  - **Встановлення** — вибери «Двері» в меню (`Tab`) і натисни `ПКМ`, дивлячись
    на тверду підлогу: двері (полотно з фільонкою, напівпрозорим віконцем і
    ручкою; спільні геометрії/матеріали) стають колоною 1×2 і повертаються
    стулкою до гравця.
  - **Відчинити/зачинити** — `ПКМ` по дверях (з будь-яким предметом у руці):
    стулка плавно обертається на завісах із процедурним рипом (скрип смуги
    шуму + тон, при зачиненні — дерев'яний стук). Зачинити двері на гравцеві,
    тварині чи нечисті не вийде — перевірка перетину AABB.
  - **Тверда перешкода** — зачинена стулка блокує обидві клітинки колони через
    спільний `moveEntityAxis`, тож гравець, тварини, зомбі, кріпери й скелети
    не проходять: уночі нечисть юрмиться під дверима, але будинок — безпечний.
    Стріли (гравця й скелетів) **встромляються** в зачинені двері, а не
    пролітають крізь них.
  - **Зняття й валідація** — `ЛКМ` по дверях знімає їх (тріски + звук дерева);
    двері спадають, якщо зруйнована опора під ними чи їхню колону зайняв блок
    (вибух, падіння сипкого блока, встановлення) — `validateDoors()` на всіх
    чотирьох шляхах оновлення світу. Смолоскип/посів/ліжко/драбина не ставляться
    в клітинки дверей і навпаки.
  - **Збереження** — двері зберігаються зі світом (формат
    `[x, y, z, dx, dz, open]`), зокрема стан відчинено/зачинено; старі сейви
    сумісні (поле необов'язкове).
  - **Нове досягнення** «🚪 Домовласник» — поставити двері (тепер **38**
    здобутків; мета-«Майстер MineClone» враховує і його). Дебаг:
    `MCDebug.giveDoor()`, `doorNear()`, `toggleNearDoor()`, `doors`,
    `doorBlocked(x, y, z)`.

## 2026-07-06

### Додано
- **Скло: прозорий будівельний блок.** Новий блок «Скло» (`GLASS`) — вікна в
  будинках, теплиці, підводні тунелі й оглядові вежі. Звичайний воксель
  (тверда фізика, як камінь), але рендериться окремим напівпрозорим мешем:
  - **Прозорість** — альфа-канал процедурного тайла атласу (світла рамка по
    периметру, ледь помітні діагональні відблиски, майже прозоре нутро);
    окремий матеріал скла з `transparent` і вимкненим `depthWrite`, щоб ближчі
    шибки не «затирали» дальші. Нуль зовнішніх ассетів.
  - **Вітраж без швів** — грані між сусідніми скляними блоками не малюються
    (нові правила видимості граней у мешері чанків); непрозорі блоки та
    вода/лава впритул до скла тепер показують свою грань — крізь шибку видно
    все, зокрема товщу води за склом акваріума.
  - **Видобуток і звук** — б'ється швидко (0.35 с), із дзвінким «кришталевим»
    звуком (високочастотний фільтр у процедурному синтезі).
  - **Нове досягнення** «🪟 Вікно у світ» — поставити скляний блок
    (тепер **37** здобутків; мета-«Майстер MineClone» враховує і його).
  - На мінімапі скло позначається світло-блакитним; старі сейви сумісні.
    Дебаг: `MCDebug.giveGlass()`, `glassWallNear()`, `look(yaw, pitch)`.

## 2026-07-05

### Додано
- **Драбини: вертикальне лазіння.** Новий окремий предмет «Драбина» (`LADDER`) —
  шахти, вежі й скелі тепер долаються без стовпів із блоків чи польоту. Сутність
  за перевіреним патерном смолоскипа (модель + мапа клітинок), а не воксельний
  блок — нуль зовнішніх ассетів і жодних правок сітки чанків:
  - **Встановлення** — вибери «Драбина» в меню (`Tab`) і натисни `ПКМ`, дивлячись
    на **бічну грань** твердого блока: драбина (дві стійки + щаблі, спільні
    геометрії/матеріал) чіпляється до стіни в клітинці перед нею. Клік по
    підлозі/стелі шукає будь-яку тверду сусідню стіну як запасну опору.
  - **Лазіння** — коли AABB гравця перетинає клітинку драбини: `W` чи `Space`
    (сенсор — джойстик уперед) — угору (3.2 бл/с), `Shift` — униз, без клавіш —
    **повільне сповзання** (−1.5 бл/с). Гравітація не діє, **шкоди від падіння
    немає** (арка падіння гаситься щокадру), щаблі порипують деревом
    (`Sound.step(LOG)` за накопиченим шляхом).
  - **Зняття й валідація** — `ЛКМ` по драбині знімає її (тріски + звук дерева);
    драбина спадає, якщо зруйнована стіна-опора або її клітинку зайняв блок
    (вибух, потік, встановлення) — `validateLadders()` на всіх чотирьох шляхах
    оновлення світу, як у смолоскипів. Смолоскип/посів/ліжко не ставляться в
    клітинку драбини й навпаки.
  - **Збереження** — розставлені драбини зберігаються зі світом (формат
    `[x, y, z, dx, dz]`); старі сейви сумісні (поле необов'язкове).
  - **Нове досягнення** «🪜 Верхолаз» — піднятися драбиною на ~2.5 блока
    (тепер **36** здобутків; мета-«Майстер MineClone» враховує і його).
    Дебаг: `MCDebug.giveLadder()`, `ladderColumnNear(h)`, `ladders`, `climbed`,
    `onLadder`.

## 2026-07-04

### Додано
- **Човни: подорож водою.** Новий окремий предмет «Човен» (`BOAT`) — довгоочікуваний
  спосіб швидко пересуватися морем і озерами замість повільного плавання. Окрема
  сутність (модель із коробок + власна фізика), а не воксельний блок — тож нуль
  зовнішніх ассетів і жодних правок сітки чанків:
  - **Спуск на воду** — вибери «Човен» у меню (`Tab`) і натисни `ПКМ`, дивлячись
    на воду чи землю: човен спливає окремою моделлю на **поверхні води** (проста
    плавучість тримає його на ватерлінії), а на суходолі підкоряється гравітації.
  - **Посадка й кермування** — `ПКМ` по човну (з будь-яким предметом у руці) —
    сісти в нього. Кермуй **поглядом**: `W` — вперед, `S` — назад; човен пливе
    помітно **швидше, ніж уплав** (до 6.4 бл/с). Колізії з рельєфом — через
    спільний `moveEntityAxis`; поки пливеш, **немає шкоди від падіння**. `Space`
    (на сенсорі — кнопка ⬆) — злізти; злазиш також автоматично при загибелі.
  - **Плавучість і дрейф** — вільні човни гойдаються на хвилях і поступово гасять
    дрейф; човен, що виплив на мілину чи берег, осідає на землю.
  - **Збереження** — розставлені човни зберігаються зі світом (позиція + курс);
    формат сейва **сумісний зі старими світами** (поле необов'язкове).
  - **Нове досягнення** «🚣 Мореплавець» — уперше відплисти на човні (тепер **35**
    здобутків; мета-«Майстер MineClone» враховує і його). Дебаг:
    `MCDebug.giveBoat()`, `placeBoatNear()`, `mountBoat()`, `dismount()`, `boat`,
    `boats`, `riding`, `press(code)` / `release(code)`.

## 2026-07-03

### Додано
- **Відро: перенесення води й лави.** Новий окремий предмет «Відро» (`BUCKET`)
  дає змогу **носити джерела флюїдів** — сплітає докупи наявну симуляцію води й
  лави, звук, частинки, хотбар і досягнення; нуль зовнішніх ассетів:
  - **Набір** — наведись на **джерело води чи лави** й натисни `ПКМ`: клітинка
    джерела стає повітрям (сусідні потоки висихають), а порожнє відро **стає
    повним** — окремий стан-id у слоті («Відро з водою» / «Відро з лавою») з
    власною процедурною іконкою. Набрати можна лише **вічне джерело**, а не
    потік (`findFluidSource` іде вздовж погляду до першого джерела, зупиняючись
    на твердому блоці).
  - **Вилив** — повним відром натисни `ПКМ` по грані блока (як ставлять блок):
    у націлену клітинку **ставиться джерело**, і `setBlock` одразу запускає
    звичайну симуляцію потоку; відро знову порожніє. Лити можна лише в повітря
    чи флюїд і **не в самого гравця**. Вилита **лава поряд із водою застигає в
    камінь** (наявна реакція флюїдів).
  - **Стан у хотбарі** зберігається зі світом (порожнє/з водою/з лавою — це
    просто id у слоті), формат сейва **не змінено** (старі світи сумісні).
  - **Нове досягнення** «🪣 Водовоз» — уперше набрати флюїд у відро (тепер
    **34** здобутки; мета-«Майстер MineClone» враховує і його). Дебаг:
    `MCDebug.giveBucket('empty'|'water'|'lava')`, `useBucket()`, `held`,
    `heldId`, `viewRay()`.

## 2026-07-02

### Додано
- **Творчий політ (подвійний `Space`).** Швидкий спосіб будувати, оглядати світ
  і рятуватися від небезпеки — самодостатнє розширення фізики гравця, нуль
  зовнішніх ассетів:
  - **Перемикання** — **подвійний тап `Space`** вмикає/вимикає політ (на сенсорі —
    подвійний тап кнопки стрибка ⬆). Автоповтор клавіші ігнорується, а меню
    блоків/досягнень не заважає.
  - **У польоті** гравітація вимкнена: `Space` підіймає, `Shift` опускає, без
    клавіш гравець **зависає** на місці. Горизонтальний рух трохи швидший (7 бл/с),
    колізії зі стінами зберігаються, а **шкоди від падіння немає** (арка падіння
    скидається щокадру). Вимкнеш політ у повітрі — гравець падає з поточної
    висоти вже за звичайними правилами.
  - **Скидання/збереження** — політ вимикається при відродженні; стан
    зберігається у сейві (формат сумісний зі старими світами — поле необов'язкове).
  - **Нове досягнення** «🕊 Політ» — здійнятися вперше (тепер **33** здобутки;
    мета-«Майстер MineClone» враховує і його). HUD показує позначку `✈ Політ`.
    Дебаг: `MCDebug.toggleFly()`, `MCDebug.flying`.
- **Риболовля: вудка, поплавок і риба в торбу їжі.** Новий окремий предмет
  «Вудка» (`ROD`) дає спокійне, відновлюване джерело харчу поряд із полюванням
  і фермою — сплітає докупи наявні системи води, частинок, звуку, голоду та
  досягнень, нуль зовнішніх ассетів:
  - **Закид** — наведись на воду й натисни `ЛКМ`: уздовж погляду шукається
    поверхня води (`findBobberTarget`), туди «падає» **червоно-білий поплавок**
    (сутність у світовій сцені, не змінює воксельну сітку), а від кінчика
    вудлища тягнеться **волосінь**. Модель вудки — від першої особи (вудлище +
    котушка), з легким замахом при закиданні. Закид не над водою нічого не ловить.
  - **Клювання** — за 2.5–9 с (у дощ швидше — риба активніша) поплавок
    **сіпається під воду** з плюскотом частинок і тихим «бульком» (Web Audio).
    Встигни натиснути `ЛКМ` ще раз у **вікні підсічки** (~1.3 с) — і риба з
    висхідним «дзинь» удачі йде **просто в торбу їжі** (+5 одиниць, поїсти `F`),
    як здобич із полювання. Проґавиш вікно — риба зривається, чекай наступного.
  - **Автозмотування** — зміна предмета в хотбарі чи загибель гравця самі
    змотують вудку; риболовля не змінює формат сейва (старі світи сумісні).
  - **Нове досягнення** «🎣 Рибалка» — упіймати першу рибу (тепер **32** здобутки
    разом із лавовим; мета-«Майстер MineClone» враховує і його). Дебаг:
    `MCDebug.giveRod()`, `castRod()`, `forceBite()`, `reelRod()`.
  - Вудка, поплавок, волосінь та іконка **мальовані процедурно** (жодних ассетів).

## 2026-07-01

### Виправлено
- **Стабільність потоку лави (QA після деплою).** Вертикальний струмінь лави
  «мигав» між рівнями потоку без зупину, бо рівень джерела лави збігався зі
  стелею рівня потоку — це давало нескінченне переписування блоків і постійну
  перебудову мешів чанків (мікрофризи). Схему рівнів вирівняно за водою
  (джерело=4 над стелею потоку=3), тож струмені лави тепер **стабілізуються**
  (черга текучості спорожняється), а коротшу дальність розливу задає жорсткіший
  поріг у `tickLavaCell`, а не менші числа рівнів.

### Додано
- **Лава — гарячий флюїд у надрах.** Новий рідинний блок, що доповнює воду й
  робить глибокі печери по-справжньому небезпечними. Нуль зовнішніх ассетів:
  - **Розжарена текстура, що світиться** — лава мальована процедурно (помаранчева
    маса з темною кіркою та яскравими жовтими прожилками) і рендериться окремим
    проходом матеріалом, який **не залежить від освітлення**, тож лава жевріє і
    вдень, і вночі.
  - **Динамічне світло** — пул точкових ламп щокадру (з тротлінгом) призначається
    найближчим відкритим клітинкам лави, заливаючи печеру теплим мерехтливим
    сяйвом. Світять лише озера поблизу — без обходу всіх чанків.
  - **Текучість** — джерело + три рівні потоку. Лава тече **вниз і вбік, як вода,
    але повільніше й недалеко** (менша дальність розтікання). Потік без підживлення
    висихає; джерело вічне. Поставлена лава одразу тече.
  - **Горіння** — дотик тілом підпалює гравця: помаранчевий пульсуючий спалах на
    екрані, періодична шкода й **язики полум'я**, що догорають ще кілька секунд
    навіть після виходу з лави (нова причина смерті «Згорів у лаві»). У лаві рух
    в'язкий і повільний, занурюєшся й вибираєшся ледь-ледь. Лава так само палить
    **зомбі, кріперів, скелетів і тварин**.
  - **Реакція з водою** — лава, що торкнулася води, **застигає в камінь** (пара,
    сичання); лава, що тече на воду, теж каменіє. Це дає безпечний спосіб гасити
    потоки.
  - **Генерація** — статичні **озера лави заливають дно печер** у надрах (до
    y=8), тож спуск у глибини (та досягнення «Надра землі») тепер вимагає обачності.
  - **Інтеграція** — лаву можна поставити з меню блоків (`Tab`); нею можна
    **замуровувати** мости, а поставлений блок падає/тече як флюїд. Нове звукове
    оформлення (глухе гудіння озера, сичання пари) синтезується через Web Audio.
    Новий здобуток **«Пекуче знайомство»** (обпектися лавою) — усього досягнень 31.
  - **Дебаг** — `MCDebug.pourLava(up)` виливає джерело поряд, `igniteMe()` підпалює
    гравця, `lavaAt(x,y,z)` / `blockAt` читають воксель, `fire` показує час горіння,
    `lavaQueue` — розмір черги текучості, `setBlock(x,y,z,id)` — довільна зміна.

## 2026-06-30

### Додано
- **Гравітація сипких блоків і новий блок «Гравій».** Пісок більше не висить у
  повітрі — тепер пісок і гравій **підкоряються гравітації**:
  - **Падіння окремою сутністю** — коли під сипким блоком зникає тверда опора
    (видобуток сусіда, вибух динаміту/кріпера, потік води), блок зривається й
    **падає кубом із власною текстурою**, прискорюючись (gravity), а досягнувши
    першої твердої поверхні — **знову стає вокселем** із легким пилом і глухим
    звуком приземлення. Падіння **ланцюгове**: прибравши блок із-під стовпа
    піску/гравію, увесь стовп осідає донизу.
  - **Запускає лише оновлення блоків** — згенерований рельєф (піщані дюни,
    пляжі, дно озер) лишається на місці, бо падіння ініціюється **тільки** зміною
    блока поряд (видобуток, встановлення, вибух, рух води), а не завантаженням
    світу. Сипкий блок, поставлений у повітрі, одразу падає; падає й крізь воду,
    осідаючи на дно.
  - **Гравій** — новий воксельний блок: сірувата сипка текстура з темними й
    світлими камінцями (мальована процедурно, нуль зовнішніх ассетів).
    Генерується **широкими кишенями в камені** на будь-якій глибині (~6%
    каменю), добувається швидше за камінь і доступний у меню блоків (`Tab`) та
    для встановлення — як і будь-який сипкий блок, він падатиме без опори.
  - **Інтеграція** — падіння вписане в наявні системи: вибух підкопує опору й
    сусідній пісок/гравій осипається в кратер; приземлення коректно валідує
    смолоскипи, посіви та ліжка, що могли опинитися під блоком; стан зберігається
    зі світом (приземлені блоки — звичайні воксельні зміни).
  - **Дебаг** — `MCDebug.dropBlock(id, up)` кидає сипкий блок на `up` клітинок над
    поверхнею (для тестів), `MCDebug.blockAt(x,y,z)` читає воксель, а
    `MCDebug.falling` показує кількість блоків у польоті.

## 2026-06-29

### Додано
- **Досягнення (Advancements): прогрес-система, що сплітає докупи всі механіки.**
  Пісочниця нарешті дістала цілі — 30 здобутків, що відзначають перші кроки й
  майстерність у кожній наявній системі (видобуток, бій, ферма, мандри,
  виживання), нуль зовнішніх ассетів:
  - **Спливаюче сповіщення** — здобувши досягнення, гравець бачить золоту
    «плашку» вгорі екрана («Досягнення здобуто!» + назва) з короткою висхідною
    **фанфарою** (Web Audio, синтезований мажорний акорд). Сповіщення
    **стають у чергу** й самі зникають, тож кілька здобутків поспіль не
    перекривають одне одного.
  - **Журнал** (клавіша **`J`** або кнопка **🏆** праворуч) — модальна панель із
    сіткою всіх досягнень: відкриті світяться золотим (іконка + назва + опис),
    закриті заблоковані (🔒, назва прихована — лишається підказка-завдання).
    Угорі — лічильник прогресу **X / 30**. Закрити `J`/`Esc`/✕ або кліком повз
    панель. На десктопі панель звільняє курсор (світ на паузі), як меню блоків.
  - **30 здобутків** охоплюють: видобуток (перший блок, деревина, кожна з чотирьох
    руд, мета-«Геолог» за всі руди), будівництво, динаміт, смолоскипи, ферму
    (посів + урожай), їжу, полювання й лук, поранення та смерть, перемоги над
    **зомбі / кріпером / скелетом**, сон у ліжку, спуск у надра (y<6) і підкорення
    вершин (y>52), плавання, відвідини **кожного з 4 біомів** і мета-«Картограф»,
    а вінчає все **«Майстер MineClone»** за всі інші здобутки.
  - **Збереження окремо від світу** — здобутки лежать у власному ключі
    `localStorage` (`mineclone:achievements`), тож «Нова гра» чи зміна версії
    сейва їх **не чіпають** — це наскрізні досягнення. Здобутий статус ідемпотентний
    (повторне виконання завдання нічого не дублює).
  - **Дебаг** — `MCDebug.achievements`, `MCDebug.achProgress`, `MCDebug.unlock(id)`,
    `MCDebug.unlockAll()`, `MCDebug.resetAchievements()` і `MCDebug.toggleAchPanel()`
    для ручного тестування з консолі.

## 2026-06-28

### Додано
- **Мінімапа та компас: топ-даун карта місцевості для орієнтування у світі.**
  Біоми додали різноманіття й орієнтири — тепер їх видно з висоти пташиного
  польоту, не сходячи з місця. Фіча сплітає докупи наявні системи рельєфу,
  біомів, тварин, нечисті та ліжок:
  - **Кругла мінімапа вгорі праворуч** малюється на `<canvas>` **процедурно** —
    нуль зовнішніх ассетів. Поверхню семплимо тими ж функціями, що й генератор
    світу (`heightAt`/`biomeAt`), **без генерації чанків** — швидко й без
    сайд-ефектів, тож карта показує й ще не завантажені регіони попереду.
  - **Колір за біомом** — рівнина та ліс зеленими відтінками, пустеля піщаним,
    тундра білим, вода синьою (глибше — темніше), пляжі піском. Легка **рельєфна
    тінь** (світло з північного заходу) надає пагорбам об'єму.
  - **Зміни гравця** — поставлені та зруйновані блоки визирають на мінімапі
    поверх процедурної поверхні (будівлі, вежі, шахти видно одразу).
  - **Маркери сутностей** — тварини (за кольором виду), нічна нечисть (червоні
    крапки) й поставлені ліжка позначені в межах огляду; гравець — білий
    трикутник у центрі, що **обертається за напрямком погляду**.
  - **Компас** — північ (−Z, напрям погляду при `yaw=0`) завжди догори; мітки
    **Пн/Сх/Пд/Зх** по колу. Унизу — поточний радіус огляду.
  - **Три рівні масштабу** (±32 / ±56 / ±88 блоки) перемикаються **тапом/кліком**
    по мінімапі; клавіша **`N`** ховає/показує її. Вибір масштабу й видимість
    зберігаються в `localStorage` **окремо від світу** (версія сейва не
    змінюється — старі світи сумісні).
  - **Продуктивність** — важкий ресемплінг растрового поля throttle-иться до
    ~5 Гц, а маркери малюються щокадру поверх кешованого поля, тож рух плавний.
  - **Дебаг** — `MCDebug.toggleMinimap()` і `MCDebug.minimapZoom(0|1|2)` для
    ручного тестування з консолі.

## 2026-06-27

### Додано
- **Біоми: світ поділено на регіони з власною поверхнею та рослинністю.**
  Раніше карта була одноманітним зеленим килимом — тепер генератор світу
  розводить великі регіони з різним кліматом, що додає різноманіття й
  орієнтири для мандрів:
  - **Чотири біоми** — **Рівнина** (рідкі дерева, трава), **Ліс** (густі
    дерева), **Пустеля** (пісок до кількох шарів углиб, кактуси, без дерев) і
    **Снігова тундра** (снігова поверхня, поодинокі засніжені дерева). Біом
    колони задає **низькочастотний шум** «температури», а помірний пояс
    додатково ділить «вологість» на ліс і рівнину — регіони ~140 блоків, тож
    переходи плавні й безшовні між чанками.
  - **Два нові блоки, мальовані процедурно** — **Сніг** (поверхня тундри) і
    **Кактус** (стовпи 1–3 блоки в пустелі). Обидва доступні в меню блоків
    (`Tab`), добуваються та ставляться як звичайні блоки — нуль зовнішніх
    ассетів. Атлас текстур розширено з 4×4 до 4×6 тайлів.
  - **Біомна рослинність** — щільність дерев тепер залежить від біому (густий
    ліс, рідка рівнина, поодинокі дерева в тундрі, жодного в пустелі), а
    кактуси ростуть лише на сухій поверхні пустелі.
  - **Пляжі лишилися піщаними** незалежно від біому, а печери, руди й береги
    озер працюють як раніше — біом змінює тільки поверхню та рослинність.
  - **Сумісність зі снігопадом** — снігова тундра природно поєдналася з уже
    наявною погодою (сніг на висоті) і добовим циклом.
  - **Дебаг** — `MCDebug.biome` показує біом під гравцем, а
    `MCDebug.tpBiome('desert'|'snowy'|'forest'|'plains')` телепортує до
    найближчого сухого регіону цього біому.
  - **Збереження** — версію генератора піднято до `v4` (нова поверхня тих
    самих координат робить старі сейви несумісними).
- **Ліжка та сон: проспати небезпечну ніч до світанку й закріпити точку
  відродження.** Уночі тепер полюють зомбі, кріпери й скелети — з'явився спосіб
  її пережити. Фіча сплітає докупи наявні системи доби, ворогів, частинок,
  звуку, відродження та збереження світу:
  - **Новий предмет «Ліжко»** у меню блоків (`Tab`). Іконка й 3D-модель
    (дерев'яний каркас на ніжках, червоний матрац, біла подушка) мальовані
    **процедурно** — нуль зовнішніх ассетів.
  - **Встановлення** — як смолоскип чи посів, ліжко є **окремою сутністю**, а
    не воксельним блоком: `ПКМ` ставить його на тверду опору (не змінюючи
    воксельну сітку й не блокуючи рух), повертаючи за напрямком погляду.
  - **Сон** — `ПКМ` по поставленому ліжку **вночі** (той самий поріг темряви,
    що й спавн нечисті) присипляє гравця: екран **плавно тьмяніє**, час
    перескакує до **світанку** — і нічна нечисть на сонці згоряє. За дня чи
    коли поряд (≈8 блоків) є монстри, заснути не вийде (з'являється підказка).
  - **Точка відродження** — сон **закріплює спавн** біля ліжка: після загибелі
    гравець оживає тут, а не на стартовому спавні.
  - **Відпочинок** — прокинувшись ситим, гравець трохи **відновлює здоров'я**.
  - **Цілісність і збереження** — ліжко **спадає**, якщо зруйнувати/підірвати
    опору під ним чи зайняти його клітинку; позиція та орієнтація ліжок і
    точка відродження зберігаються разом зі світом.
  - **Дебаг** — `MCDebug.placeBed()`, `MCDebug.sleep()`, `MCDebug.clearMobs()`,
    а також `MCDebug.beds`, `MCDebug.sleeping`, `MCDebug.spawnPoint`.

## 2026-06-26

### Додано
- **Скелети: нічні лучники з дальнім боєм і тактикою кайтингу.** Бій нарешті
  став двостороннім — тепер не лише гравець стріляє, а й по ньому. Фіча
  сплітає докупи наявні системи ворогів, снарядів-стріл, частинок, звуку й
  смерті гравця:
  - **Новий ворог «Скелет»** з'являється вночі поряд із зомбі та кріперами
    (≈чверть нічної нечисті). Кістлява гуманоїдна модель (череп із темними
    западинами, тонкі кінцівки) тримає **процедурний лук** у правій руці —
    нуль зовнішніх ассетів.
  - **Кайтинг (тримання дистанції)** — на відміну від зомбі, скелет не лізе
    впритул: **наближається**, якщо гравець далі ~11 блоків, **відступає**,
    якщо ближче ~4.5, і **завмирає**, щоб стріляти, у проміжку. Перестрибує
    перешкоди й плаває, як інша нечисть.
  - **Постріл з лука** — маючи **лінію зору** (груба перевірка відрізка на
    тверді блоки) і в межах ~16 блоків, скелет піднімає лук (видимий «тель»
    ~0.25 с) і пускає **стрілу-снаряд** із перезарядкою ~1.8–2.6 с.
  - **Стріли ворога ранять гравця** — стріли позначені прапорцем `fromMob`:
    повторно використовують ту саму систему снарядів (гравітація, підкроки,
    встромляння у блоки), але влучають у **гравця** (≈3 шкоди + відкид у
    напрямі польоту), а не в інших істот. Постріл наводиться у груди гравця
    з **компенсацією падіння стріли** та легким розкидом (скелет — не снайпер).
  - **Слабкості** — як і зомбі, скелети **згоряють на світанку**, а світло
    смолоскипа їх **відлякує**. Ховання за рогом збиває їм лінію зору й приціл.
  - **Звук і смерть** — постріл синтезується наявним `Sound.bowShoot`,
    загибель лишає кістляво-білу хмарку частинок; новий екран смерті
    «Застрелений скелетом».
  - **Дебаг** — `MCDebug.spawnSkeleton(n)` створює скелетів поряд із гравцем.

## 2026-06-25

### Додано
- **Лук і стріли: дальній бій із натягом і снарядами-стрілами.** Бойова
  система здобула дистанцію — тепер можна стріляти, а не лише бити впритул.
  Фіча сплітає докупи наявні системи бою, частинок, звуку, фізики сутностей
  і збереження хотбара:
  - **Новий предмет «Лук»** у меню блоків (`Tab`). Іконка мальована
    процедурно (дуга + тятива + накладена стріла), без атласних текстур.
  - **Натягування** — з луком в активному слоті `ЛКМ` **утримують**, щоб
    натягнути (сила росте ~0.85 с до повного), і **відпускають**, щоб пустити
    стрілу. Модель лука від першої особи показує натяг: стріла й тятива
    відходять назад, лук піднімається до прицілу, а повністю натягнутий —
    легко тремтить. Ледь натягнутий лук пострілу не дає.
  - **Стріла-снаряд** — окрема короткоживуча сутність (за зразком динаміту):
    летить **із гравітацією**, орієнтуючись уздовж швидкості, і рухається
    підкроками, щоб на високій швидкості не «прострілювати» блоки чи істот.
    Модель (древко, вістря, оперення) змодельована процедурно.
  - **Шкода на відстані** — стріла б'є **зомбі, кріперів і тварин**: сила й
    швидкість зростають із натягом (**3–8** одиниць — повний натяг сильніший
    за ближній удар у 5), з відкидом у напрямі польоту. Збита тварина лишає
    м'ясо так само, як від ближнього удару (спільний код шкоди істотам).
  - **Встромляння** — стріла, що влучила в блок, **застрягає** в ньому
    (хмарка частинок у кольорі блока, глухий «тук») і за ~12 с зникає; промах
    зникає за ~8 с або при падінні за межі світу. Воксельна сітка не
    змінюється.
  - **Звук** — натяг (рип дерева й тятиви), постріл (клац + свист, гучніший
    від сили) і влучання в блок синтезуються через Web Audio (нуль аудіофайлів).
  - **Дебаг** — `MCDebug.giveBow()` кладе лук у поточний слот,
    `MCDebug.shootArrow(power)` стріляє із заданою силою, `MCDebug.arrows`
    показує кількість стріл у світі.

### Виправлено
- **Орієнтація моделей кирки й лука від першої особи.** Обидва предмети
  малювалися «боком», і їхній робочий бік не дивився на ціль:
  - **Кирка** показувала широку грань голівки, а гострі дзьоби стирчали
    вбік — здавалося, що вона б'є боком, а не вістрям. Тепер голівку
    повернуто навколо вертикалі, тож передній дзьоб дивиться на блок
    (углиб екрана), а сама кирка читається як кирка, а не молоток.
  - **Лук** лежав у площині, оберненій до камери, тож була видна вся пласка
    «дуга». Тепер дугу розгорнуто у вертикальну площину вздовж прицілу — її
    видно з ребра збоку, стріла дивиться вперед крізь лук, а під час натягу
    тятива згинається в «галочку», тягнучи стрілу назад до гравця.

## 2026-06-24

### Додано
- **Землеробство: грядки, посіви та відновлюване джерело їжі.** Виживання
  здобуло мирну альтернативу полюванню — власну ферму, що сплітає докупи
  наявні системи їжі/голоду, циклу день/ніч, світла смолоскипів, частинок і
  збереження світу:
  - **Новий предмет «Насіння»** у меню блоків (`Tab`). Іконка мальована
    процедурно (грунт + паростки й зерна), без атласних текстур.
  - **Посів** — `ПКМ` із насінням садить його на верх блока трави чи землі.
    Це окрема сутність (хрест із двох площин), що **не змінює воксельну
    сітку** й не блокує рух — за зразком смолоскипів.
  - **Зростання у 5 стадій** (паросток → золотий колос) — кожна стадія має
    власну процедурну текстуру (колір від зеленого до золотого, поява
    колоса-голівки). Темп залежить від **світла**: швидше під сонцем,
    повільніше в негоду, а вночі посів трохи живить **смолоскип поблизу**
    (у радіусі ~7 блоків). Стебла легко **погойдуються на «вітрі»**.
  - **Збір урожаю** — `ЛКМ` по **дозрілому** колосу додає їжу в ту саму
    торбу (бейдж 🍖), яку їдять клавішею `F`, тож ферма стає **відновлюваним**
    харчем на додачу до м'яса тварин. Незрілий посів зривається без зерна.
  - **Цілісність** — посів спадає (хмарка зелених частинок), якщо зруйнувати
    грунт під ним, підірвати його чи зайняти його клітинку блоком.
  - **Збереження** — координати, стадія й прогрес росту всіх посівів
    зберігаються разом зі світом.
  - **Дебаг** — `MCDebug.crops` показує кількість, `MCDebug.plantNear(n)`
    засіває грядку поряд, `MCDebug.growCrops()` миттєво доводить усе до зрілості.

## 2026-06-23

### Додано
- **Кріпери: тихі підривники, що детонують поряд із гравцем.** Нічна нечисть
  здобула напарника, який сплітає докупи наявні системи зомбі, вибуху динаміту,
  частинок і звуку:
  - **Новий ворог** — зелений чотириногий силует із характерним «обличчям»,
    змодельований процедурно (нуль зовнішніх ассетів). Зʼявляється вночі тими
    самими правилами, що й зомбі (≈третина спавнів — кріпери), але, на відміну
    від них, **не згоряє вдень** — лишається загрозою цілодобово.
  - **Поведінка-засідка** — мовчки переслідує гравця, а зблизька
    (~2.7 блока) **запалює ґніт**: завмирає, **сичить** (синтез Web Audio),
    набрякає (до ×1.35) і дедалі частіше блимає білим.
  - **Вибух** — за ~1.4 с ґноту кріпер детонує **тим самим вибухом, що й
    динаміт** (`explode()`): руйнує блоки сферою, відкидає й ранить гравця,
    тварин і сусідній динаміт, здіймає дим та іскри. Смерть у вибуху має
    окрему причину на екрані смерті — «Підірваний кріпером».
  - **Контргра** — відбіжиш далі ~4.2 блока, і ґніт **згасне**, а кріпер
    знову перейде в переслідування; встигнеш ударити `ЛКМ` двічі до детонації —
    кріпер загине **без вибуху** (хмарка зелених частинок).
  - **Світло стримує** — як і зомбі, кріпери не зʼявляються в радіусі
    смолоскипа, тож освітлена база лишається безпечною.
  - **Дебаг** — `MCDebug.spawnCreeper(n)` викликає кріперів поряд із гравцем.

## 2026-06-22

### Додано
- **Смолоскипи: процедурне джерело світла, що ставиться у світ.** Темрява ночі
  й печер тепер долається не лише втечею — її можна освітити:
  - **Новий предмет «Смолоскип»** у меню блоків (`Tab`) і за замовчуванням
    в останньому слоті хотбара (`0`). Іконка малюється процедурно (держак +
    полум'я), без атласних текстур.
  - **Встановлення на опору** — ПКМ ставить смолоскип на верх блока (підлогу)
    або на бічну грань (стіну, з нахилом полум'я назовні). Це окрема сутність:
    смолоскип **не змінює воксельну сітку** й не блокує рух.
  - **Динамічне світло** — пул із кількох точкових ламп щокадру призначається
    найближчим смолоскипам, тож камінь, тварини й зомбі довкола реально
    освітлюються; полум'я **мерехтить**, гало пульсує, а вгору зрідка злітають
    розжарені іскри (на наявному `InstancedMesh` частинок).
  - **Стримування зомбі** — у радіусі ~7 блоків від смолоскипа вороги більше
    **не зʼявляються**, тож освітлена база стає безпечною.
  - **Зняти** смолоскип можна ЛКМ (як і блок); він також **спадає**, коли
    зникає його опора (видобуток, вибух) або коли його клітинку зайняв блок.
  - **Звук** — м'яке потріскування полум'я при встановленні та ледь чутний
    тріск поблизу (синтез через Web Audio, без аудіофайлів).
  - **Збереження** — позиції та кріплення всіх смолоскипів зберігаються разом
    зі світом; `MCDebug.torches` показує їх кількість.

## 2026-06-21

### Додано
- **Динамічна погода: дощ, сніг і грози.** Небо й світ ожили негодою, що
  сплітає докупи наявні системи хмар, частинок, звуку та циклу день/ніч:
  - **Опади** — періодично налітає дощ (тонкі сині штрихи) або сніг (м'які
    білі сніжинки, що погойдуються), що падають колоною навколо гравця.
    Тип залежить від висоти: високо в горах холодно — там сніжить, нижче
    йде дощ. Усе на одному `InstancedMesh` (один виклик відмалювання), без
    зовнішніх ассетів; краплі за рельєфом приховані перевіркою глибини.
  - **Стихія в небі** — під час негоди небо темнішає й сіріє, туман
    густішає, а хмари наливаються свинцем; затемнення плавно наростає
    й згасає разом із силою опадів.
  - **Гроза** — сильну зливу під відкритим небом раз у раз пронизує
    **спалах блискавки** з відкладеним **гуркотом грому** (Web Audio).
  - **Звук дощу** — безперервний ambient-шелест, синтезований зацикленим
    фільтрованим шумом; гучність стежить за силою дощу (сніг беззвучний).
  - **Укриття** — під дахом і в печерах опадів і шуму немає (перевірка
    відкритого неба над гравцем).
  - **Збереження** — стан погоди (тип, таймер, сила) зберігається разом зі
    світом; консольні `MCDebug.setWeather('rain'|'snow'|'clear')` і
    `MCDebug.weather` допомагають у тестуванні.

- **Голод і їжа: повний цикл виживання через полювання.** Систему здоров'я
  доповнено голодом, що замикає виживання в петлю «полюй → їж → відновлюйся»:
  - **Шкала голоду** — десять піксельних «курячих ніжок» у HUD під сердцями.
    Голод спадає від активності (біг і стрибки виснажують швидше за ходьбу),
    стоячи на місці — не витрачається.
  - **Полювання** — тепер тварин (свинки, корови, кури) можна бити `ЛКМ`/⛏,
    як і зомбі: жертва спалахує червоним, **панікує й тікає** геть від гравця
    прискорено, а загинувши — розсипається хмаркою м'ясних частинок і лишає
    **сире м'ясо**.
  - **Торба їжі** — зібране м'ясо рахується в бейджі 🍖 (праворуч унизу).
    Клавіша **F** (чи кнопка 🍖 на сенсорі) з'їдає порцію та поповнює голод;
    між поїданнями — коротка пауза, із характерним звуком жування.
  - **Звʼязок зі здоров'ям** — природна регенерація працює лише ситим
    (голод ≥ 16) і «спалює» їжу; коли шкала голоду порожня — починається
    **голодування** (повільна шкода, але не до смерті, мінімум одне серце).
  - **Збереження** — рівень голоду й запас м'яса зберігаються разом зі світом.


- **Хмари: процедурні білборди, що дрейфують небом.** Небосхил оживає — над
  світом тепер пливе шар пухнастих хмар:
  - **Процедурна текстура** (накладені м'які кружки на canvas) — жодних
    зовнішніх ассетів, у дусі решти проєкту.
  - **18 хмар-білбордів** розставлено по всьому небосхилу (елевація 10°–44°);
    усі ділять один матеріал, тож оновлення тону/прозорості коштує один виклик
    за кадр.
  - **Дрейф за вітром** — увесь шар повільно обертається навколо вертикалі.
  - **Реакція на час доби** — хмари білі вдень, темно-сині вночі та теплі
    (оранжеві) на сході/заході; прозорість зменшується в темряві.
  - Малюються тим самим «небесним» проходом, що й сонце/місяць/зорі — без
    туману й незалежно від дальності промальовування; коректно перекриваються
    рельєфом.

- **Звук: процедурні ефекти через Web Audio API.** Раніше світ був повністю
  беззвучним — тепер дії гравця та середовище звучать, причому **жоден звук не
  завантажується файлом**: усе синтезується на льоту (осцилятори + білий шум +
  фільтри), у дусі решти проєкту (нуль зовнішніх ассетів):
  - **Кроки** — короткий приглушений «цок» у такт ходьбі, тембр залежить від
    матеріалу під ногами (трава, пісок, дерево, камінь звучать по-різному).
  - **Видобуток** — ритмічне «цокання» киркою під час руйнування та виразний
    тріск у момент, коли блок ламається; **встановлення** блока — короткий стук.
    Тембр теж залежить від типу блока.
  - **Стрибок і приземлення**, **сплеск** при зануренні у воду.
  - **Динаміт** — тихе тиктакання ґнота й потужний низькочастотний вибух.
  - **Бій** — глухий удар при влучанні по зомбі, передсмертний стогін ворога,
    а також **випадкові стогони** зомбі, коли вони неподалік у темряві.
  - **Шкода гравцю** — різкий звук при отриманні ушкоджень.
  - **Вимикач звуку** — кнопка 🔊/🔇 у куті екрана та клавіша **M**; стан
    зберігається разом зі світом. Звук вмикається після першої дії гравця
    (вимога браузерів до автозапуску аудіо).

## 2026-06-20

### Додано
- **Вороги: зомбі та ближній бій.** Світ уночі став небезпечним — день/ніч тепер
  має значення:
  - **Зомбі зʼявляються в темряві** на твердій поверхні неподалік від гравця (до 6
    одночасно) і **переслідують** його, перестрибуючи перешкоди й обходячи рельєф;
    модель — гуманоїд із простягнутими вперед руками, що погойдуються при ходьбі.
  - **Атака зомбі** — при контакті ворог завдає шкоди та відкидає гравця; шкода
    проходить через наявну систему здоровʼя (червона вінʼєтка, кадри невразливості,
    екран смерті з причиною «Розтерзаний зомбі»).
  - **Ближній бій** — **ЛКМ** (або кнопка ⛏ на сенсорі) тепер не лише добуває блоки:
    якщо в прицілі поблизу є зомбі, удар влучає по ньому — ворог спалахує червоним,
    відлітає від удару, а після кількох ударів гине хмаркою зелених частинок.
  - **Горіння на світанку** — удень незахищені зомбі займаються вогнем (дим
    процедурними частинками) і швидко згоряють, тож ранок приносить полегшення.
  - **Консольний діагностичний інтерфейс** `window.MCDebug` (`night()`, `day()`,
    `spawnZombie(n)`) для швидкої перевірки циклу доби та ворогів.

## 2026-06-19

### Додано
- **Система виживання: здоров'я, повітря та смерть.** Раніше гравець був невразливим —
  тепер світ може завдати шкоди:
  - **Здоров'я (10 сердець).** Шкала сердець у HUD над хотбаром; серця малюються
    процедурно на canvas (повне / половина / порожнє), без зовнішніх ассетів.
  - **Шкода від падіння** — падіння вище за 3 блоки відбирає здоров'я пропорційно
    висоті; приземлення у воду шкоди не завдає.
  - **Потоплення** — коли голова під водою, витрачається запас повітря (шкала
    бульбашок з'являється під час занурення); коли повітря закінчується, гравець
    починає задихатися й втрачати здоров'я. Над водою повітря швидко відновлюється.
  - **Шкода від вибуху** — близький вибух динаміту тепер не лише відкидає, а й ранить
    гравця залежно від відстані.
  - **Природна регенерація** — якщо кілька секунд не отримувати шкоди (і не тонути),
    здоров'я поступово відновлюється.
  - **Червоний спалах** по краях екрана при отриманні шкоди (вінʼєтка).
  - **Екран смерті** з причиною загибелі та кнопкою «Відродитися», яка повертає
    гравця на точку спавну з повним здоров'ям. Поточне здоров'я зберігається разом
    зі світом.

### Змінено
- **Хотбар тепер завжди в один рядок** (10 слотів), щоб коректно вмістити шкалу
  здоров'я над ним; на вузьких екранах слоти трохи зменшено.

## 2026-06-18

### Додано
- **Система частинок.** Дії гравця тепер супроводжуються процедурними частинками
  (без зовнішніх ассетів), які малюються одним `InstancedMesh` — лише один виклик
  відмалювання, тож продуктивність не страждає:
  - **Уламки при руйнуванні** — після видобутку блока з його місця вилітає хмарка
    дрібних кубиків, забарвлених усередненим кольором текстури цього блока; вони
    падають із гравітацією, осідають на твердій поверхні й плавно зникають.
  - **Пил при встановленні** — короткий легкий сплеск у кольорі поставленого блока.
  - **Дим і іскри від вибуху** — динаміт здіймає сірий дим, що піднімається й
    розсіюється, та жмут розжарених помаранчевих іскор, що розлітаються й гаснуть.

## 2026-06-17

### Додано
- **Меню вибору блока (`Tab`).** Раніше всі 13 блоків були в хотбарі, тож руди потрапляли
  на слоти 10–13, яких немає на клавіатурі. Тепер:
  - **Меню всіх блоків** відкривається по `Tab` (на сенсорі — кнопка ▤): сітка з усіма
    доступними блоками; клік призначає блок у активний слот хотбара. Закривається по
    `Tab`/`Esc`, кнопкою ✕ або кліком по тлу. На десктопі меню звільняє курсор без переходу в паузу.
  - **Хотбар скорочено до 10 слотів** швидкого доступу (`1`–`9` та `0`); вміст слотів
    можна налаштовувати через меню та зберігається разом зі світом.

### Додано (раніше)
- **Небесні тіла в циклі день/ніч.** Раніше небо лише плавно змінювало колір — тепер
  над світом видно:
  - **Диск сонця** — тепле адитивне світіння, що рухається небосхилом і зникає за обрієм;
  - **Місяць** — бліде ядро з кратерами та м'яким гало, з'являється навпроти сонця вночі;
  - **Зоряне небо** — ≈1400 процедурних зір на «небесній сфері», які повільно обертаються
    разом із циклом доби, плавно проявляються вночі й ледь помітно мерехтять;
  - **Сяйво на сході/заході** — небо тепліє до помаранчевого, коли сонце близько до обрію.

  Усе малюється процедурно (без зовнішніх ассетів) окремим проходом рендера на нескінченній
  «небесній сфері» з власною камерою, тож не підпадає під туман і дальність промальовування.
